# HybridGuard Agent/RAG 行动指南入口

原 1993 行总指导书已按任务领域拆分，详细内容位于：

- [hybridguard_agent_rag_guide/README.md](hybridguard_agent_rag_guide/README.md)

## Agent 强制阅读规则

1. 本文件只是根目录入口，不包含详细方法。
2. Agent 必须先读取目录中的 README。
3. 普通任务只读取 README 路由的一份主分册。
4. 跨域任务最多读取两份分册，且必须符合 README 中的组合白名单。
5. 首轮只定位并读取相关 Anchor 小节，不默认通读整册。
6. 需要三份以上分册时，应拆成多个子任务。
7. 禁止一次性读取 01–09 全部分册。

原总稿内容已全部迁移到分册中，本文件不再作为任务执行的知识正文。


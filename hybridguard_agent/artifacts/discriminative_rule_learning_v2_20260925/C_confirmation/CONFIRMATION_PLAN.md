# V2-C 独立确认计划（尚未执行）

当前状态：`DEVELOPMENT_COMPLETE_CONFIRMATION_PENDING`。本文件是下一次授权的执行方案；没有采集、读取或评分新独立确认材料。以下待决项未获用户决定前，不开始确认。

**数据角色与研究单位。** 新增材料在进入模型开发前登记为 `NEW_DEVELOPMENT` 或 `INDEPENDENT_CONFIRMATION`，两者分区、分批次、分访问权限。用于补足 screen/timezone 支持、选择规则或调阈值的材料属于新增开发，不能同时确认同一模型。现有 54 组三态/162 阶段以及本轮 LOEO、LOCO、重代入材料均已暴露。100 个描述阶段、UNKNOWN 控制和未标注 MTC 不自动成为正常监督数据。

每条材料记录实际设备、环境、OS、App、WebView/浏览器/内核版本、采集批次及配置；设备与环境使用可追溯的研究化名，记录关联关系。主配置跨多个实际独立环境，环境也交叉配置，避免某个配置只绑定一个默认值。每个主配置至少两个实际独立环境只是解除单环境绑定的结构下限，并非充分样本量。相同环境重复三态只增加重复观测，不能增加独立设备数；同设备不同版本和不同时区保留关联。需另行确认主要配置集合、独立组边界和可行交叉矩阵。

**正常侧优先清单。** 先确认研究定义允许哪些合法变化，再从真实可采集设备/版本中覆盖下表；结构探针不充当真实样本。

| 优先材料 | 需要记录和核实 |
|---|---|
| 高/低内存、不同核数 | Web 近似/截断值与 Native 可用物理内存的单位和口径；真实浏览器 hardwareConcurrency 限制；不能把两者当精确相等 |
| 单语言/多语言、顺序、应用语言 | 浏览器首选语言和列表、Native locale；应用与系统合法不同；区分数量变化和语义变化 |
| MIME/PDF/plugin 能力 | 实际浏览器版本、PDF 支持与返回的计数；不凭计数构造不存在的列表 |
| UA/platform 与获准自动化 | 定制目的、Host 配置/默认 UA、实际 Web UA/platform、硬件平台和授权范围；合法自动化不因 webdriver 为 true 就改标攻击 |
| 非零时区与 DST | 同一时点、Native 区域/偏移和 Web minutes-west；跨零点及 DST 边界；符号、单位、可用性分别记录 |
| 第二来源与测量缺失 | 若保留 C0 对照，记录 Native/Host 的合法变化、有效性门控和故障；保持原 U/FAILED 语义 |

**采集与标签。** 获得另行授权后，先保存采集合同、预期成员、环境/配置交叉表与操作范围；再采集 pre / intervention / post 三态。中间态只有在执行事实与研究定义成立时才记攻击；合法变化可以是中间态正常材料，不以名称预判标签。保留操作日志、效果证据、恢复证据、质量状态、缺失、失败和每阶段时间；post 不因安排了恢复动作就自动认定 clean。由独立于模型输出的裁决者依据预先定义给标签，分开记录操作成功、效果确认、恢复成功和检测表现。合法场景不能因为报警被事后重标为攻击，证据不足保持 UNKNOWN/NOT_ADJUDICATED。

用 [confirmation_record_template.json](confirmation_record_template.json) 登记身份、角色、配对、授权、执行、质量和标签依据。模板没有真实记录，也不启动采集。特征与评价侧信息分开保存；部署预测只接收当前允许字段，所有 ID、phase、标签、版本/路径、操作声明及历史 pre/未来 post 只作管理/分层信息。

**冻结与开封。** 本轮待确认模型已经保存如下；确认前在新的执行合同中登记实际模型文件、模型 ID、[METHOD_FREEZE.json](METHOD_FREEZE.json)、[C_CONTRACT.json](C_CONTRACT.json)、对应源码版本与测量合同。若改学习程序或重训，则产生新的候选身份，不能沿用本次冻结名义。

| 程序 | 已冻结模型 ID | 文件 |
|---|---|---|
| C0__GREEDY_OR | `v2c-40ffc55faff1962cc57829ee` | [model.json](trials/V2-C__ALL_DEVELOPMENT_162__C0__GREEDY_OR__attempt01/model.json) |
| W0__GREEDY_OR | `v2c-3def521e987075a27b52e2df` | [model.json](trials/V2-C__ALL_DEVELOPMENT_162__W0__GREEDY_OR__attempt01/model.json) |
| W0__R_KEEP_V1 | `v2c-af691341a2d38032d88973b5` | [model.json](trials/V2-C__ALL_DEVELOPMENT_162__W0__R_KEEP_V1__attempt01/model.json) |

W0 两个实例的规则、编码器、阈值相同；确认时保留两个程序身份，说明这一事实，不制造虚假的独立比较。三模型固定后才允许访问获授权确认特征，先保存关闭预测，再按授权开封评价标签。不得把冻结前已接触的材料重新命名为未接触材料；本轮对新材料只允许身份/元数据层面的核对，没有内容/标签开封权限。

**预先评价与排除规则。** 拟采用固定三模型的配对比较，按预登记 expected-ID 分母报告攻击微检出、配置内先平均环境再平均配置的宏检出、pre/post/合并 clean 报警、决定/原子/子句覆盖、U/EMPTY/FAILED、完整 F-T-F、条件恢复及复杂度；逐配置/环境/设备/版本/批次报告新增与丢失。合法变化中间态单列为真实正常侧分层，是否纳入主 clean 目标及其权重须先冻结，不冒充本轮 108 个控制。

未解决的目标均为 `TBD_BY_USER`：最低微/宏检出、最大 pre/post/合并及合法变化报警、最低覆盖、容许失败、比较优效/非劣或纯描述目标、置信水平和区间方法、主要配置与权重、独立组/样本量、采集成本与停止日期。训练约束 alpha=0.05 不是自动获批的总体误报目标。本轮不根据当前分数替用户设门槛。

原则上不因模型失败/弃判/不利得分排除已接纳成员；保留在分母。重复身份、损坏原始采集、效果无法确认等 QC 情形必须在看分数前按合同登记；不能重采直到通过。标签未知的材料保持独立类别，不算 clean；缺失和工程失败同时报告。具体接纳、排除及重采上限在新合同中先冻结。

样本量尚不具备可担保的依据：正常侧总体分布、独立组内相关性和目标差值未定。规划时可用 `3 × r × sum(K 个配置各自的独立环境数)` 估算配对阶段数，另加真实合法变化与 QC 失败所需材料；实际成本包括设备/版本可达性、一次三态的执行与恢复、裁决时间。`r` 为同环境重复数，不能替代独立组数。可以先对仅作 NEW_DEVELOPMENT 的可行性材料估计成本；若据此修改程序，需重新冻结并另备未接触确认材料。不承诺显著性或足够功效，不使用合成探针估计真实 FPR 或置信区间。

**停止规则。** 只在另行获授权、待决目标和预期成员全部冻结后执行一次固定评价，保留不利结果。达到采集/预算/时间上限或预声明停止条件即关闭，材料不足报告未完成；不按分数追加组别直至好看。若确认暴露问题，报告并停止；后续改方法时，该批确认材料转为开发反馈，新确认必须使用另行授权的独立材料。R10/V3、采集、攻击工具、标签开封、付费服务和 Git 发布均未由本轮方案自动授权。

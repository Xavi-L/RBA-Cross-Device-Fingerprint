# 配置迁移结论的适用范围

本轮唯一新评价轨为R08_CONFIG_TRANSFER / LOCO-v1：14个配置整体留出，14个GREEDY_OR/OP05训练折及各折两固定基线。配置名和字段集合只用于训练侧支持统计与独立评价侧表，不作推理特征或机制标签。

- **可以陈述：** 在已暴露的App177受控材料上，每折排除目标配置全部三态与关联包后重新学习；按冻结划分得到54个攻击和108个pre/post阶段上的配置迁移结果。模型保存、加载和冻结在该折首次外层特征读取前，标签在预测关闭后独立连接。
- **共享环境：** 14折训练侧均含3个环境组；每折outer环境全部在训练侧出现，安装关联也共享。bundle、session、triplet在两侧不交叉；R01声明的LOCO关联边界原样保留。不能叫独立环境验证或独立物理设备验证。共有历史采集campaign仍存在。
- **机制未验证：** R08_MECHANISM = NOT_EVALUABLE_UNVERIFIED_MECHANISM_PARTITION，mechanism_id/result=null。工具名、配置名、声明修改字段集合不自动代表不同攻击机制。configuration_environment_support.csv只给配置×环境支持，机制表不填伪标签或0性能。
- **没有前瞻材料：** R08_PROSPECTIVE = NOT_AVAILABLE，结果null。没有新最终模型、确认集访问、重新切分旧材料填补前瞻列或补采。
- **分轨而非增加样本：** R05 LOEO回答环境关联组留出，R08 LOCO回答共享环境中的配置留出。相同162阶段上的两轨预测不叠成324独立样本；三方法的486记录也不等于486独立阶段。相同决策及规则签名不能证明两种隔离问题等价。
- **标签范围：** clean仅指S01认可的声明干预表面。54个pre和54个post不是108独立设备；100描述阶段、UNKNOWN时间对照和未标注MTC不进入监督分母。观察零报警不保证总体FPR为零。DIRECT_CORE_OR的clean全弃判不能作为低误报证据。
- **暴露与探索：** 现有材料已经历历史分析，这是回顾性配置分组评价，不能洗成全新盲测。R07单层/跨层互补和集合并集36只保留旧诊断；本步没有单层、联合、集成、补造规则、阈值调优或V2。未根据外层结果重排作业、换参数或重试选优。

R07父步骤保持PARTIALLY_COMPLETED，其未运行/不可用分支不是本轮前置。R08仅配置主分支完成等待验收；机制与前瞻缺口保留，不阻止报告本轮受限结果，也不自动授权R09。

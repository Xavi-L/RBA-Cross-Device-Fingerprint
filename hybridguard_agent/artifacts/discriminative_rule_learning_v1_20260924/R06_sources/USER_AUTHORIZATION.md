# 本次用户明确授权

用户已外部验收 R05（9376c775549e913e0e88b678d345a35fdd726081），仅授权冻结的 R06_SOURCE_REFIT：3 LOEO 折 × 8 来源条件 × GREEDY_OR/OP05，来源位序 O_u/H/E。21 个新增 fit，24 个模型单元，1,296 个预测记录，162 个唯一监督阶段。SRC-111 三折精确复用 R05 模型和已关闭预测，禁止重拟合、改模型 ID/字节/创建时间。

必须使用 R04_freeze_r1 的冻结源码、配置、解释器与依赖，经原 dispatcher 核对精确作业及 train 成员。当前快照研究协议、冻结/资源摘要由启动前实际文件校验绑定在 R06_APPROVAL.json。继续原 REAL_RESEARCH_BUDGET 账本，不重置，不混用合成账本，保留 R05 artifact_directory。

仅本来源重训练主分支获授权。拟合后来源删除为预留未实现分支，标 NOT_RUN，不能临时修改冻结源码执行。不得按外层效果更改检测方法、候选、门控、支持度、alpha、复杂度、划分或操作点；技术失败保留原尝试，不自动重试选优。后处理只读保存产物，不补算预测。UNKNOWN 时间对照及未标注 MTC 不进入监督分母。

完成后停止等待验收；不自动执行 R07、其他分支、全开发集重拟合、补采、攻击工具、旧 S07、提交或推送。

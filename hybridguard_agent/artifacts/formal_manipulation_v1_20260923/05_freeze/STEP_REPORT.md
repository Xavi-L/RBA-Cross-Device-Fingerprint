# S05 STEP_REPORT

工程状态：PASS；范围：运行前静态冻结。先按用户授权将 S04 的 74 个文件提交推送至 `origin/main`，远端和本地 HEAD 都是 `f00e699d2763afed5369774f01e5a0f632d8f5c6`；270 个无关工作区状态条目保留。随后仅执行 S05，S05 本身未提交或推送。

## 依赖与时点

S01/S02 的事实、262 阶段适配与报告相符；S03 的 57 项来源和 S03-R 的 7 候选/5 家族分别保留其版本；S04 最终 35 项测试、14 合成预测状态行和验收一致。旧 S03 零候选、S03-R 单关系 probe、S04 合成结果不混为同一种验收。旧报告“仍在本地”“等待审查”为原完成时点，本轮不改历史，不重跑上游。

发现并显式修订的是图表规格差异：S04 Fig.6 占位文字与总计划操作点图不一致。另补齐 S02 null configuration_id 到 S04 汇总键的评估侧映射。详见 `S05_PROTOCOL_FREEZE_v2.md`；这些修订不改变原事实或检测行为。没有将实质验收冲突改成通过。

## 冻结结果

合同 `formal-manipulation-relation-risk-attribution-v2`；策略 `formal-manipulation-family-or-v2`；协议 `formal-manipulation-protocol-v2`。独立配置 `config/formal_manipulation_protocol_v2/`；output/config-dir 同时显式指定。原 S03/S03-R/S04 配置、原目录和 P0–P6 不覆盖。

| 对象 | 数量/去向 |
|---|---|
| 输入/适配成功/拒绝 | 262 / 262 / 0，逐字节复用 S02 |
| cohort | 准入攻击三态162、UNKNOWN时间对照54、低证据45、不完整1 |
| 包/阶段关联 | 31 包、88 关联；空包继续引用 S01 inventory，不造阶段 |
| 任务分母 | 攻击54、clean_pre54、clean_post54、准入三态54；control_mid阴性0 |
| 展示分层 | 20 配置/对照键（14准入攻击配置）、13申明字段集合、3环境关联组 |
| 主矩阵 | 216 阶段 × 13 唯一变体 = 2808 单元 |
| 低证据/不完整描述矩阵 | 46 阶段 × 单一 final/App177/SRC-111 = 46 单元，仅 S10 描述 |
| 全部真实材料风险计划 | 2854 唯一单元，262 阶段全部有去向；尚未执行 |
| 首次步骤 | S06=432、S07新增1512、S08新增864、S10新增46 |
| E4 诊断计划 | 480 单元；D1/D3各216材料项，另四诊断×12合成项 |
| E4/E5 合成输入 | 49（12语义、37缺失/协调边界）；新预测0 |
| E6 抽样与重复 | 固定24阶段×2方法×(1冷+3warmup+20测量)=1152计划单元 |

全部矩阵使用同一候选/门控/阈值及逐 ID 的来源/家族定义。四来源别名和跨步骤已规划的联合/App177单元复用，不重复扩 n；相同 payload 的不同单元不去重。7 候选、5 家族，E/O_u/H/C=23/9/14/11。O_u/E 共享、H/C 零候选的结构限制明确，不预写收益。current-v3 合法风险视图仍别名于 final，不伪造第三个检测器。

对照配对没有可登记的跨包共同批次键，54 准入三态全部明确 unmatched；不凭日期、别名或得分造配对。时间对照保留三时点，但 no_intervention=UNKNOWN，不取得 FPR。Fig.6 以主 control_mid FPR 为横轴，当前关闭；不偷换阴性总体。clean_pre/post 可在其有限已准入范围分别评价。

## 隔离与版本

推理输入位于 `blind/`，完整 facts/原索引/准入/历史路径位于 `evaluation/`。新汇总索引只在评估侧补确定的配置键、API、申明字段集合和包级批次，原 S02 与所有 admission_fact 保持。预测完成并关闭、预定单元唯一对账后才可 join，标签不回传。静态步骤计划不能被 S04 runnable-job schema 接受，也不授予 S06 权限。

一次有限 manifest 绑定 135 文件，其中参与源码44、运行配置/字段资源25；源副本、原输入副本与原文件一致。S05 新代码并未包含在推送的 S04 HEAD 中，已通过未跟踪文件清单与实际副本标明，未声称工作区干净。冻结摘要为 `d1eb5d9894ff8a9194692f8bcfdc22b7f6c603448f1f21ad8151980a3fb19d27`。仅参与文件版本绑定，没有全库重哈希或安全审计。

P6 原协议和 `CONSUMED_FIXED_EVALUATION`、117 主代表/18保留重复的消费时点均复制保存。App 轨是暴露回放；P6是保存输出复用，不新分割或新盲测。S05 未读取 P6 的逐行效果或任何真实正式预测来定规则/条件。自然 MTC 缺失分支本协议仅做已有分类复用；新的 MTC worker 运行需另版本先定输入单元。

## 验证

14/14 聚焦静态测试通过：事实冲突拒绝、标签不授资格、重复/缺失 ID 拒绝、相同 payload 独立保留、2854 单元及别名去重、混版本/缺配置/阈值变化拒绝、双目的地/历史路径/符号链接保护、固定抽样、合成与真实诊断域、评估补键不改事实、Fig.6 缺标签处理、静态源码闭包及禁止运行模块导入、严格 JSON。

初轮 14 项中有 1 个断言失败和 1 个错误：目录保护夹具同时触发重叠/已存在两种条件，断言期待了后者；源码闭包首次拒绝了合法包内相对 import。分别隔离夹具条件、按 Python 包语义解析 AST 后通过；保留初轮日志，没有运行检测修复效果。更早静态开发探查修正了绑定文件没有逐行 contract_version 的字段假设和控制行 annotation=null 的处理，均在输出目录创建前失败。具体见 DEVELOPMENT_CHECK_HISTORY。

生成后有限 manifest/schema/集合验收通过：262 阶段唯一映射、所有分母可由原 S01 表重建、2854 单元与行号相符、辅助单元唯一、原事实一致、无规则/运行/验证模块加载、无 predictions/rule_events。282 个登记既有文件 size/mtime 不变，工作区无关条目保留。该历史保护检查不是密码学身份认证。

## 仍保留的限制与停止点

真实候选覆盖、TPR/FPR、来源收益、恢复效果、缺失效果、成本和解释质量均未评价。14 个 S04 合成保存预测是历史记录；本步真实/合成新预测均为0，门控/规则/风险运行0，LLM0，采集0，阈值变更0。7候选可执行合同不代表真实材料已有检测能力或性能。

时间对照标签、缺原始日志、L1证据范围、3环境组非独立物理设备、Native相对参照、合法自定义/OEM/渲染路径反例、无独立Browser及攻击归因UNKNOWN均保留。没有补采、补日志、补标或重挖。

S05 DONE/PASS 后停止，等待审查及 S06 或其他单步授权；不进入正式实验，不自动提交或推送 S05。完成时点与最终检查见 VALIDATION.json。

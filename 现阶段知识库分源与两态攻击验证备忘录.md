# 阶段性备忘录：知识库分源、两态攻击验证与大规模字段效应分析

**生效范围：** 后续知识库整理、真机采集、攻击侧验证与字段效应分析。
**当前目标：** 将知识库按来源显式拆为“官方文档知识”和“真机数据挖掘规则”两类；分别在正常真机数据与攻击工具数据上验证，目标是正常真机不报警、已验证施加攻击的样本成功报警。

## 1. 本阶段的核心原则

1. 这里的“两类”是**知识来源分类**，不是把原有的功能层、证据层或实现形式全部推倒重来。每条知识都必须有一个明确的 `source_type`，检索、推理、统计和报告时均保留该标签。
2. 官方文档用于解释字段语义、合法差异、适用范围和容错边界；它不能因为“来自官方”就被直接当作风险阈值。
3. 真机数据挖掘出的规则属于经验性知识。它必须保留产生它的数据范围、支持样本、反例和适用条件；在攻击侧验证成功前，不能宣称它具有攻击检测能力。
4. 原始 feature 发生变化、规则被触发、最终产生攻击报警是三个不同层次的事实。不能把任意字段差异直接等同于攻击，也不能把工具成功启动等同于攻击效果已被观察到。
5. 缺失、不可用或不满足采集前提的字段应标为“未评估”，不能被当作“未变化”或“正常”。

## 2. 知识库的来源分类

| 来源类型 | 含义 | 可承担的作用 | 必须保存的依据 | 不能作出的越界结论 |
|---|---|---|---|---|
| `official_document`（官方文档知识） | 来自 Android、Chrome、WebView 等官方技术文档的字段定义、API 行为、安全机制、限制和合法差异说明。 | 字段释义、关系约束、容错条件、适用版本和官方依据。 | 发布方、URL、文档版本/抓取时间、对应字段、短摘要、推理强度、适用范围。 | 不能在缺少项目内验证的情况下直接推出项目风险阈值或攻击报警率。 |
| `device_mined_rule`（真机数据挖掘规则） | 从已收集的真机 feature 中归纳出的稳定关系、偏离模式或阈值候选；后续可结合受控攻击成对数据验证其攻击相关性。 | 生成可执行 predicate、正常边界、候选异常模式和反例。 | 数据集/批次与特征版本、生成数据切分、字段列表、规则表达式、支持度、反例、适用设备范围、人工审核记录。 | 不能把仅在某个设备、厂商型号、采集方或工具版本中出现的现象泛化为通用攻击规则。 |

同一条规则可以同时引用官方文档和真机数据，但必须指定一个**主来源类型**，并把另一类依据列为补充证据。两类知识不得合并成一个无来源的“知识库命中数”，必须分别统计和报告。

### 2.1 官方来源内部的执行层次

知识来源仍然只有“官方文档”和“真机数据挖掘”两类，但官方文档进入风险判定时必须再区分两个执行层次：

| 执行层次 | 含义 | 当前处理方式 |
|---|---|---|
| `official_direct` | 官方文档直接定义的安全/完整性 verdict，例如经服务端验证的 Play Integrity、Key Attestation 结果。 | 当前相关字段尚未采集，不执行，不虚构为 0 报警。 |
| `official_derived_semantic_rule` | 官方文档定义字段/API 语义，本项目据此显式推导跨层相等、兼容、蕴含或上下文关系。 | 必须记录官方来源、项目推导、premise fields、容错、反例和验证状态；只有不依赖未校准阈值的关系才可执行。 |
| `device_mined_rule` | 从真机数据、攻击模板或既有实验形成的经验 predicate。 | 独立执行和报告，不与官方派生关系合并计数。 |

`official_derived_semantic_rule` 的来源仍是官方文档，但 predicate 是**项目推导**，不能表述成 Google/Android 官方给出的攻击结论。它也不应混入数据挖掘规则的经验阈值；一旦关系需要数值容差、设备族基线或支持度门槛，应保持候选状态，等待正常数据校准。

## 3. 攻击验证口径：由三态改为两态

攻击工具的实现、执行证据和字段目录继续以 [hybridguard-browser-fingerprint-research](hybridguard-browser-fingerprint-research/README.md) 为详细参考。本阶段将后续实验的观察状态统一为：

```text
baseline（攻击前）  ->  attack_active（施加攻击后）
```

不再采集或验收 `attack_revoked` / `clean_post`（攻击撤销后）状态，也不再要求“攻击撤销完成后的数据必须与攻击前一致”。后续报告只能说明攻击前与施加攻击后之间实际观察到的 feature 差异，不能再用“已恢复”作为结论或正例资格条件。

已有三态记录、历史回滚证据和旧标注协议仍按其原始口径保存，不能删除、改写或与两态实验直接合并计算。尤其是现有 [attack_effect_annotation_mapping_v1.md](hybridguard-browser-fingerprint-research/execution_log/protocols/attack_effect_annotation_mapping_v1.md) 仍包含可切换干预的回滚要求；在开始两态采集前，应新增并冻结一个版本化的两态协议，而不是静默放宽旧协议。

### 3.1 成对比较单位

对可开关的攻击方式，`baseline` 与 `attack_active` 应尽量来自同一物理设备、同一宿主/容器配置、同一 feature schema 和相同采集流程，只改变已记录的攻击干预。

对无法在同一会话中诚实开关的持久环境（例如某些模拟器、虚拟化或多开环境），可以使用预先定义的 `matched_baseline`，但必须明确标注其不是同设备开关配对，不得伪造攻击撤销后的恢复状态，也不得将其与严格成对结果混为同一证据等级。

每个有效比较至少应可追溯到：比较 ID、阶段、采集时间、稳定设备/环境标识、feature schema 版本、攻击工具名称与版本、配置/runner、执行证据、可用字段状态，以及原始记录位置。

### 3.2 两态攻击效果的最小证据

一个攻击样本只有同时满足以下条件，才可计入“攻击侧成功报警”的候选分母：

1. 攻击工具的执行状态已被独立证据确认，而不是仅凭进程启动或脚本返回值推断；
2. `baseline` 与 `attack_active` 之间至少有一个目录内 `field_path` 出现可复核的变化；
3. 变化能够与本次干预关联，且已排除明显的采集失败、字段缺失、环境切换或正常波动；
4. 相应来源类型的知识实际被执行并产生了可审计的规则命中/报警依据。

“工具运行成功但字段未变”应报告为“未观察到目录内字段效果”，不能被记为攻击检测成功。

## 4. 两类知识必须分别完成的验证

| 知识来源 | 正常真机数据验证 | 攻击工具数据验证 | 独立报告结论 |
|---|---|---|---|
| 官方文档知识 | 在有效、正常的真机采集上不产生报警；若触发，应检查是否误读官方语义、容错边界或采集条件。 | 在已验证的攻击成对数据上，若官方语义约束确实被违反，应能给出与字段和官方依据对应的报警。 | 官方语义和容错是否适配本项目数据；不把它表述为官方给出的风险阈值。 |
| 真机数据挖掘规则 | 在用于验证的正常真机数据上不报警；若报警，应保留为反例并回查规则的设备范围、特征稳定性和采集质量。 | 在已验证的攻击成对数据上，规则应能命中实际受攻击改变的 feature，并给出具体 predicate 与对比证据。 | 规则在哪些设备、工具、版本与配置下有效；不能越过观测范围宣称通用性。 |

两个验证面都要报告分母和排除原因：正常真机的有效记录数、报警数、未评估数；攻击侧的有效比较数、成功报警数、未观察到字段效果数、执行未证实数和字段缺失数。不要只给一个混合后的“准确率”或“报警率”。

## 5. 正常真机与攻击侧的执行顺序

1. **冻结字段与来源卡片。** 为每条知识补齐来源类型、字段映射、适用范围、容错和版本；未标明来源的知识不得进入正式验证。
2. **先验证正常真机不报警。** 在真实设备的重复采集和跨设备采集上观察自然波动与合法差异；出现报警时不能静默删除样本，必须标注为规则问题、正常边界、采集问题或待调查问题。
3. **建立两态攻击比较。** 每个攻击配置先采集 `baseline`，再施加攻击并采集 `attack_active`；保留完整执行与配置证据。
4. **分别运行两类知识。** 对同一有效比较分别输出官方文档来源结果和真机挖掘规则结果；官方来源内部再把 `official_direct` 与 `official_derived_semantic_rule` 分开，所有统计保留执行层次，不能合并成一个知识库命中数。
5. **复核字段差异。** 先确认到底哪些 `field_path` 发生了什么变化，再判断规则是否合理；规则结论不能反过来替代原始差异证据。
6. **规则晋级。** 只有同时通过正常真机边界检查、攻击侧成对验证、反例检查和人工审核的 `device_mined_rule`，才可从候选规则晋级为发布规则。

为避免信息泄漏，数据挖掘规则只能从预先指定的训练/开发数据生成；用于最终验证的正常真机数据和攻击成对数据不得反过来参与规则生成。评估单位应是独立的有效设备/环境比较，而不是把同一设备的重复采集、同型号记录或单条 session 当作彼此独立的真机样本。

## 6. 大规模真机数据后的重点工作：攻击工具稳定改变量

在获得足量真机数据后，下一阶段不是只统计“是否报警”，而是建立**攻击工具—配置—feature 效应表**。推荐流程如下：

1. 对大规模正常真机数据完成质量控制、重复设备标识和自然波动分析，冻结用于攻击实验的 feature schema 与基线知识版本。
2. 按攻击工具、工具版本、具体配置和适用环境制定可复现实验计划；每个计划均保留两态原始记录与执行证据。
3. 在可行时对同一真实设备执行 `baseline -> attack_active`；无法严格配对时单列为 `matched_baseline`，不与严格配对样本混算。
4. 按 `工具 × 版本 × 配置 × field_path` 汇总字段变化，而不是只按“某工具是否报警”汇总。
5. 对每个字段输出：有效比较数、变化数、未变化数、缺失/未评估数、变化方向或变化类型、跨独立设备覆盖数、正常重复采集波动、归因证据和当前结论状态。
6. 将结果分为“稳定改变候选”“工具/配置特异改变”“观察到但证据不足”“未观察到改变”和“未评估”；不把单次差异直接升级为稳定效应。

一个 feature 只有在预先冻结的判定门槛下，同时满足“在同一工具配置中可重复出现”“跨独立真实设备/环境仍有支持”“变化方向或变换模式一致”“不能由正常波动或采集失败更好解释”“与该次攻击干预有可追溯证据”时，才可标记为该工具的**稳定改变**。具体支持度、跨设备数和容忍阈值必须在大规模运行前写入实验协议并冻结；在此之前只能报告观察到的频率，不得人为补设阈值后宣称稳定。

即使某字段被某个工具稳定改变，结论也应限定为“该工具及该版本/配置下的稳定字段效应”。除非有跨工具、跨版本和独立验证证据，不能扩大为“所有攻击工具都会改变该 feature”或“该 feature 的变化必然代表攻击”。

## 7. 本阶段应交付的可审计产物

- 一份知识目录：每条知识的 `source_type`、字段、来源依据、适用范围、容错和版本。
- 两份分源验证结果：官方文档知识与真机数据挖掘规则分别给出正常真机不报警和攻击侧成功报警的分子、分母、排除原因与反例。
- 一份两态攻击实验清单：每项攻击工具的版本、配置、`baseline`/`attack_active` 记录、执行证据与比较 ID。
- 一份字段效应矩阵：按 `工具 × 版本 × 配置 × field_path` 记录变化统计、自然波动、归因证据和稳定性状态。
- 一份规则晋级记录：候选规则、验证证据、反例、人工审核和发布/拒绝理由。

### 7.1 2026-09-01 官方语义关联层落地结果

已新增 [官方语义关系目录](hybridguard_agent/config/official_semantic_relations.v1.json)，从 20 张官方卡片整理出 22 条带来源、容错和反例的关系：9 条为无需经验阈值的 `compiled_v1`，13 条因需要真机容差、设备族、时间序列、部署策略或未来服务端字段而保持候选/未来状态。独立执行器和验证入口分别位于 [evaluator.py](hybridguard_agent/official_semantics/evaluator.py) 与 [run_official_semantic_validation.py](hybridguard_agent/scripts/run_official_semantic_validation.py)。

冻结输入上的回顾性验证结果见 [官方知识语义关联规则验证报告](knowledge_rule_validation/official_semantic_runs/official_semantic_v1_20260901_r2/官方知识语义关联规则验证报告.md)：

- 正常输入 229 条，强关系研究语义报警 0 条；58 条出现 WebView provider/运行时 UA 主版本软偏差，229 条出现 Debuggable+Cleartext 开发上下文，均未升级为报警；18 条至少有一项关系不可判定。
- 攻击对 15 个，baseline 报警 0 个，attack_active 报警 15 个，无报警转报警 15 个；9 个 CDP 对由 Android 宿主与桌面 Web 表面矛盾触发，6 个 stealth 对由 Android Native 图形栈与明确 Windows/Direct3D WebGL 标记矛盾触发。
- 15 个报警转换全部能回连到 baseline/attack_active payload 中实际变化的 premise field；历史 15 条 `clean_post` 全部忽略。
- 该关系目录是在查看过当前字段合同和已有攻击变化后建立的，尤其 GPU/Direct3D 关系受当前 stealth 样本启发。因此 15/15 只能视为同语料回顾性执行覆盖，不能当作独立留出攻击验证、跨工具泛化或真实召回率。
- 与[既有两类知识独立验证](knowledge_rule_validation/source_isolated_runs/existing_k0_source_isolated_20260827/两类知识独立验证报告.md)中的数据挖掘轨 9/15 相比，官方派生语义层在当前冻结攻击集上覆盖更广，补上了 6 个 stealth 对；但前者为旧 predicate、后者是在看过当前攻击模式后形成的新关系，二者不是公平的独立留出对比，不能据此宣称官方派生规则本质上比数据挖掘规则更强。

### 7.2 2026-09-01 新增攻击配置重跑结果

攻击侧仓库已快进到 revision `5717fc0e8a1f76ecb6746be4fcea2a00017f50d0`。本次通过 [冻结输入清单](knowledge_rule_validation/config/official_semantic_attack_input_set.expanded_20260901.json) 选入 23 个已接受 manifest、69 个合格两态对、4 个已测工具名和 14 个精确配置。69 条历史 `clean_post` 全部只计数后忽略；一个缺少 attack_active 的 CDP manifest 和一个空 Playwright manifest 被明确排除。

两类知识仍分别执行，只在两次运行完成后做同分母对照：

- [官方派生语义层扩展报告](knowledge_rule_validation/official_semantic_runs/official_semantic_v1_expanded_20260901_r2/官方知识语义关联规则验证报告.md)：229 条正常输入强关系报警 0，其中 18 条至少一项不可判定；69 个攻击 baseline 报警 0，attack_active 报警 51，无报警转报警 51。
- [官方直接卡片/数据挖掘规则源隔离扩展报告](knowledge_rule_validation/source_isolated_runs/existing_k0_source_isolated_expanded_20260901/两类知识独立验证报告.md)：官方直接卡片仍为 0 条独立 predicate；旧数据挖掘轨在 69 个 baseline 上报警 0，attack_active 上报警 33，无报警转报警 33。它在 229 条正常输入上虽然显式报警为 0，但 229/229 都是 `insufficient_evidence`，不能当作正常通过。
- [两类知识同分母对照报告](knowledge_rule_validation/新增攻击配置两类知识独立验证对照报告_20260901.md)：33 对两轨都报，18 对只有官方派生语义层报，0 对只有数据挖掘轨报，18 对两轨都不报。仅官方派生轨报的 18 对均为 stealth 默认/WebGL 配置，由 `OFFDER-GPU-001` 补充覆盖。
- 两轨共同未报的 18 对是 WebDriver-only、resource pair、languages-only、plugins/MIME、timezone-only 和 screen-metrics-only。这些配置的执行状态和字段变化已经确认，因此它们是当前冻结关系/predicate 的覆盖缺口，不是工具失败，也不能写成安全。

因此，对“数据挖掘规则是否相对判别更强”的当前结论应为：**不是；在这份扩展受控输入上，官方派生语义层的显式覆盖更广（51 对对 33 对）。**但这只是当前冻结配置上的事实，不是两类知识本质能力的公平排名。旧 15 对曾参与语义关系设计观察；新 54 对虽在目录冻结前未被查看，但未预注册且仍来自同一受控模拟器/采集体系。

## 8. 明确不应做的事

- 不再把“攻击撤销后恢复为攻击前”作为本阶段新数据的验收要求。
- 不因旧三态记录存在而给新两态记录补造 `clean_post` 或恢复结论。
- 不把正常真机的不同型号、系统版本或合法 WebView 差异直接当作攻击。
- 不把攻击工具名称、采集方、设备型号或 App 版本当作规则捷径。
- 不把控制性攻击实验的命中率表述成真实世界欺诈率、通用攻击检测率或身份识别性能。
- 不把官方文档知识和真机数据挖掘规则合并后再掩盖其各自的失效边界。

## 9. 相关现有材料

- [知识分层、规则晋级与字段感知检索](hybridguard_agent_rag_guide/05_KNOWLEDGE_RULES_AND_RETRIEVAL.md)：既有知识卡、候选规则和晋级边界。
- [Google 官方知识库说明](google_official_kb/README.md)：现有官方来源、风险卡片与推理强度边界。
- [官方语义关联层说明与复现入口](knowledge_rule_validation/README.md)：官方直接知识、官方派生语义关系和真机数据挖掘规则的执行边界。
- [新增攻击配置两类知识独立验证对照报告](knowledge_rule_validation/新增攻击配置两类知识独立验证对照报告_20260901.md)：当前 229 条正常输入和 69 个合格两态攻击对的分轨结果及交集。
- [官方知识语义关联规则扩展验证报告](knowledge_rule_validation/official_semantic_runs/official_semantic_v1_expanded_20260901_r2/官方知识语义关联规则验证报告.md)：当前 22 条官方派生关系在扩展输入上的独立结果。
- [历史 15 对官方语义关联规则验证报告](knowledge_rule_validation/official_semantic_runs/official_semantic_v1_20260901_r2/官方知识语义关联规则验证报告.md)：保留的旧 15 对回顾性结果。
- [攻击侧项目](hybridguard-browser-fingerprint-research/README.md)：攻击工具、采集协议和执行证据的详细参考。
- [历史攻击效果标注映射 v1](hybridguard-browser-fingerprint-research/execution_log/protocols/attack_effect_annotation_mapping_v1.md)：旧三态/回滚口径的历史参考；新两态采集不得直接沿用其回滚门槛。

# HybridGuard V2 开发路线图

**主题：扩展可学习表示，以受控开发实验改善攻击检出—clean 报警—覆盖权衡**  
文档版本：`hybridguard-v2-roadmap-20260925-v1`  
制定日期：2026-09-25  
事实基线：`Xavi-L/RBA-Cross-Device-Fingerprint@5aabe2d609ab6ab737a7a057d4b9260479385fad`  
性质：后续开发指引；本资料包没有执行新的训练、预测、采集或 Git 写入。

> **调度决定：R10 暂缓，V1 保存为已评估基线，V2 进入开发主线。**
> 不补齐旧支线才能开始，不重建 R01–R09，不再复制一套十阶段准备流程。
> 允许在获授权的开发批次内修改方法、测试和运行有限实验；保留旧结果，不把开发反馈包装成未经接触的独立验证。

## 阅读导航

- 研究问题和已有证据：第 1–3 节。
- 应该改什么，以及不应该盲目改什么：第 4–8 节。
- 数据、实验设计与成功标准：第 9–11 节。
- 只有三个检查点的执行路线：第 12 节。
- 版本、资源、图表、文献与结束条件：第 13–17 节。
- 执行入口：[V2 EXECUTION_PLAN](deliverables/v2_development/EXECUTION_PLAN.md)。
- 初始状态：[V2 EXECUTION_STATUS](deliverables/v2_development/EXECUTION_STATUS.json)。
- 第一批 agent 指令：[AGENT_START_PROMPT](deliverables/v2_development/AGENT_START_PROMPT.md)。

本文将陈述分为三种：**[V1证据]**是保存结果；**[推论/假设]**是对证据的解释；**[V2设计]**是本次提出的、尚待实现和评价的方案。具体新字段、新谓词、新模型和新性能均不得因写在路线图里就视为已经存在。

---

## 1. V2 要完成什么

### 1.1 目标保持不变，方法空间允许改变

在声明的采集、设备和干预范围内，用当前会话允许的证据，学习简洁且互补的判别规则，使攻击阶段尽量报警，clean 阶段尽量不报警，同时报告弃判、失败、覆盖、复杂度和适用边界。[MAIN]

V2 的研究问题是：

> **怎样系统地构造和选择具有判别价值的关系与组合，使模型覆盖 V1 漏掉的干预，同时避免把环境差异、工具默认值和合法自定义行为当成普遍攻击特征？**

“跨层”是需要证明价值的表示方式，而不是预先获胜的约束。应认真研究扩展跨层关系，同时保留有竞争力的单表面、平铺多表面和联合表示对照。最终结果可能支持跨层主方法、混合表示，或只支持某些关系的局部价值，不预设答案。

### 1.2 本轮不做什么

不恢复旧七条报警白名单；不把理论上存在合法反例作为永久禁止学习的理由；不把所有 UNKNOWN 变成冲突；不把“规则很多”当作贡献；不为追求高 TPR 修改真值或删除漏检配置；不默认把新优化器当作候选信息缺失的解决方案。

暂不精修完整 R10 论文图、公开复现包或全部未运行支线。开发需要的诊断图和结果表可以立即生成。完整论文级整理在 V2 候选方法定型后进行。

### 1.3 本文如何覆盖旧指引

本文件覆盖 V1 文档中“必须依次做完所有 R 步骤后才可优化”的**调度限制**，不改写 V1 的研究合同、运行记录和结论。V1 的来源语义、测量与 T/F/U 定义仍是起点；任何实质语义变化都进入 V2 新版本。

本资料包不自动更新远端。安装后由 agent 在可修改的主线和执行计划顶部追加简短 V2 路由说明，保留其正文与旧状态快照；冻结目录内的同名文件完全不动。

---

## 2. V1 证据基线：优化必须从这里出发

### 2.1 材料和模型结果

**[V1证据]** 当前有 262 个阶段；162 个监督阶段由 54 个 attack、54 个 clean_pre、54 个 clean_post 组成。其余 100 个阶段保留为描述性材料。共有 14 个攻击配置和 3 个环境关联组，不能分别当成 14 种独立机制和 3 台已核验物理设备。pre/post 真值只支持既定声明干预表面；UNKNOWN 时间对照和未标注 MTC 不是自动的正常真值。[MAIN][PLAN][R08]

| 结果 | 保存事实 | 可支持的含义 |
|---|---|---|
| 旧 S06 人工资格策略 | 攻击检出 0/54 | 历史负面基线，不覆盖或删除。[S06] |
| R05 跨层主模型 | 24/54；pre/post 合计 0/108；决策覆盖 162/162 | 有局部有效性，但不能称普遍高检出或总体零误报。[R05] |
| R06 E-only | 21/54；pre/post 0/108 | 经验候选能够学出替代规则。[R06] |
| R06 O_u 及含 O_u 联合条件 | 24/54；pre/post 0/108 | 当前联合未带来额外检出，不能据此分配“官方贡献100%”。[R06] |
| R07 App Web 单层 | 27/54；pre/post 0/108；决策覆盖 162/162 | 单层具有实质竞争力。[R07] |
| R07 App Web / 跨层 | 交集15；单层独有12；跨层独有9；并集36；共同漏检18 | 存在互补；并集不是已确认的新部署模型成绩，也不是性能上限。[R07PAIR] |
| R08 配置留出 | 仍为24/54；14折都选同一关系 | 对部分已测试配置有迁移支持，但训练/测试共享环境。[R08] |
| R09 | 71次历史fit；未新增fit；固定模型计时及程序解释审计 | 开发平台与有限成本证据可复用；计时不包含原始采集和关系提取。[R09] |

R05/R08 核心模型选择的是 `DEVIATION:OFFDER-UA-001:POSITIVE`。当前 12 个 App177 原始核心候选归并成 10 个规范关系；它们不是所有可能的跨层关系。[R05][R08][GRAMMAR]

R07 的阶段微平均与配置宏平均方向不同：App Web 为 50.00% / 35.71%，跨层为 44.44% / 42.86%。App Web 三折复杂度为 12/12/2，跨层为 2/2/2。比较必须同时展示这些维度。[R07]

### 2.2 针对漏检的已知线索

R07 的 12 个新增检出来自 `w6-tool-058-legacy-default-v1`（9）和 `w9-rule-boundary-cdp-resource-pair-v1`（3）。对应保存条件涉及 MIME 数量、语言列表长度和硬件并发数；当前核心候选没有直接使用这些字段。App Web 又漏掉了跨层能够检出的三个 UA/platform 边界配置，共9个。[R07][R07PAIR]

**这只说明值得检查表示缺口，不能据此保证“补上三个字段就能抓到全部攻击”。**例如 `hardware_concurrency > 4` 是本轮学到的阈值，不是一般攻击定义。字段值可能同时反映合法设备能力、运行环境和干预，必须通过对照区分。[R07RULES]

### 2.3 不能提前认定瓶颈只有一个

**[推论/假设]** 候选表示不足是首要假设，但还可能有：有效改写未进入采集记录、测量域过窄、只表达列表长度而丢失成员内容、数值阈值候选粗糙、支持度排除专门规则、复杂度上限、优化搜索或训练分布问题。

V2-A 必须定位这些原因，而不是为了符合当前叙述，把每个漏检都标成 `MISSING_CROSS_RELATION`。

---

## 3. 两个研究层次与三个可证伪假设

### 3.1 表示与学习分开改

**表示层**决定可以观察和表达什么：跨层残差、类别组合、集合关系、已有单字段条件、适用域。

**学习层**决定选择什么：支持度、训练阈值、规则极性、OR/AND 组合、整体 clean 预算、复杂度和停止条件。

先在相同学习器与主要预算下比较表示，再在必要时改变搜索或目标；避免表示、标签、阈值、划分和优化器一起改，以致无法解释收益。

### 3.2 假设登记

| 假设 | 支持它所需的结果 | 反证或不支持的结果 |
|---|---|---|
| H1：V1 的主要限制之一是表示覆盖不足 | 新表示在开发折中增加可区分信号，新增检出可追到该信号，且保持合理 clean/覆盖权衡 | 新表示没有新增信息，或只增加正常报警 |
| H2：跨层参照比绝对单字段阈值更稳健 | 在合理单层/平铺多表面对照、合法变化压力测试或新环境中有额外收益 | 收益完全由某个单字段或恒定宿主门控解释 |
| H3：互补规则能够提升整体覆盖 | 训练侧联合学习在同样预算下取得更多独有检出，而非仅增加规则数 | 联合选择仍退化为原规则，或新增条件主要带来误报/弃判 |

原子来源 E/O_u/H/C 的问题延续，但不立即重跑所有来源组合。待候选方法有实质变化后，对最终候选做同流程来源重训练。新人工研究关系不能为了分类方便强塞进 E 或 O_u；另记 `RESEARCHER_PROPOSED_V2` 及其官方/经验依据，待审定来源后再归类。

---

## 4. V2-A 的首项工作：建立有行动指向的漏检诊断

### 4.1 诊断不是重跑材料验收

复用 S01/S02 材料索引、R02 原子矩阵、R05–R08 逐样本预测和训练日志。只对相关字段和被怀疑的谓词做定向读取；不重新遍历全部历史文件做大规模 mtime 审计，不重建旧采集链。

覆盖全部14配置，特别标识跨层漏检30、单层补检12、共同漏检18；仍包含已检出配置和相应 pre/post，防止仅对漏检设计规则。

### 4.2 诊断分类与建议动作

| reason_code | 要核实的现象 | 后续动作 |
|---|---|---|
| `NO_OBSERVABLE_CHANGE` | 允许的当前会话字段未体现已声明干预，或变化证据不足 | 保留攻击资格；说明可观测边界，必要时提出另行授权的采集检查 |
| `SIGNAL_WITHOUT_CROSS_REFERENCE` | 单层有变化，但其他层没有适当参照 | 保留单层/联合候选；不要生造跨层关系 |
| `REPRESENTATION_GAP` | 有可计算且有含义的多表面变化，当前候选没有表达 | 新版本谓词或关系模板 |
| `MEASUREMENT_UNAVAILABLE` | 参照缺失、类型无效、掩蔽或产品语义不适用 | 检查必要门控；确实缺证据则保留 U |
| `PRUNED_BY_SUPPORT` | 原子可用但未通过训练支持条件 | 在 B 中做有限支持度敏感性，不在全数据中指定最终名单 |
| `BUDGET_OR_COMPLEXITY` | 加入条件违反集合clean预算、覆盖或复杂度 | 研究组合/预算权衡，而不是删掉正常反例 |
| `SEARCH_LIMIT` | 贪心未找到，而有限同池搜索可能找到 | 对受影响表示做同池有限 IP 检查 |
| `FOLD_SHIFT` | 训练侧有用的规则在留出环境/配置失效 | 检查环境混杂和合法变化；不是改测试标签 |
| `DATA_OR_IMPLEMENTATION_QUESTION` | 记录、映射或实现不一致 | 先复现定向问题，修复需版本化；不要以性能反馈暗改事实 |

同一记录可以多标签，但必须有一个主要原因、证据路径和具体下一动作。未知原因写 `UNRESOLVED`，不强填。

### 4.3 三态改变用于诊断，不用于部署输入

在开发分析中比较 `pre → attack → post` 的实际值、质量和原子状态；区分攻击期间新增、三阶段持续、攻击无变化及恢复不明确。检测模型仍只读当前会话，不把 pre 的数值、phase 或 post 的恢复作为特征。

原始工具日志、声明修改字段和配置名仅解释干预，不进入推理特征。发现干预未观测到也不能自动从主TPR分母删掉对应攻击；可另作明确的可观测性描述。

### 4.4 增加一个有价值的可分性诊断

**[V2设计/数学推论]** 对每种固定表示，按允许的 T/F/U 向量形成相同输入组。若同一向量同时对应 attack 与 clean，确定性当前会话模型不可能在该组内同时完美检出与零误报。

报告冲突组、覆盖的攻击/clean数，并可给出该有限开发样本上、指定报警预算下的乐观可分性诊断。它不需要训练检测器，也不是测试泛化上界；更不能把43xx条方法记录当成新样本。自由文本reason、路径、session、工具或阶段不能进入向量以人为消除冲突。

同时对比“原始允许字段确实变化”和“变成规则向量后没有变化”，定位信息是在采集、编码还是规则构造时丢失。只对有明确语义的字段做这项检查，不为区分ID加入哈希或指纹唯一标识。

---

## 5. 必须比较的表示：跨层主张需要合适的对手

以下是 **V2 设计名称**，不是已经存在的运行入口。

| pool_id | 内容 | 回答的问题 |
|---|---|---|
| `C0` | V1 的10个规范跨层关系 | 原表示基线，优先复用保存结果 |
| `W0` | V1 App Web 单表面输入和训练编码 | 强单层基线，优先复用匹配的R07结果 |
| `S_FLAT` | Native/AppWeb/Host 已声明单表面原子与控制条件的平铺合集；没有额外显式跨层关系 | 增加原始信息源是否已足够？不是“单层模型” |
| `J0` | `S_FLAT ∪ C0`，统一训练规则集合 | 现有跨层关系在平铺输入之外有何增量？ |
| `CX` | `C0` 加经字段/语义核实的新跨层关系 | 扩展跨层表达本身能否改善？ |
| `JX` | `S_FLAT ∪ CX` | 混合表示是否有更好的整体权衡？ |

主方法不强制选择任意条数的跨层规则，也不在模型中硬塞一条跨层条件。若联合模型最终全选单表面条件，如实报告，并据此限制“跨层贡献”主张。

对 `S_FLAT/J0/JX`，单表面编码器均仅在对应训练折拟合；不能把R07某折中最好看的阈值跨折复用。来源别名和全局等价关系按原语义核对，同一事实不重复计支持；**只在当前样本上输出相同，不足以证明全局语义等价**。

额外保存原始/规范候选数、训练支持池大小、可用性和选中结构，避免把表示容量差异隐去。若联合池含同时表达一致与偏差的等价条件，统一极性后再处理，而不是让来源别名增加“票数”。

### 5.1 一个立即可做的开发诊断

将已保存 `C0` 与 `W0` 的同折、同ID决定按显式 OR 定义做**事后组合诊断**，核对全部162阶段、clean、U、EMPTY和失败，不只合并正例ID。

该对象命名 `POST_HOC_C0_OR_W0_DIAGNOSTIC`；使用已保存结果，不重新训练。它不是预注册主模型，也不是独立确认。与它不同，`J0` 必须在训练侧真正学习统一规则集合，不能将两个OOF模型的规则硬拼后冒充联合训练。

### 5.2 检查“名义跨层”与“实际额外信息”

某关系调用了 Native 字段，不等于该字段在当前研究域提供了额外判别信息。检查 Native 条件是否在全部样本上恒定、只是输入有效性门控，以及规范关系与同表面谓词在何种域内等价。

使用可解释的合成有效/无效域对照检查函数依赖；这只是语义测试，不算真实clean或攻击样本。实际信息增量由 `W0/S_FLAT/C0/J0/CX/JX` 的可比实验和合法变化对照判断，不能靠删除输入后变成U就证明跨层更准确。

---

## 6. 新关系应怎样产生：先核实参照，再学习参数

### 6.1 候选机会清单

下表是**待核实方向**。除明确列出的既有 Web 字段线索外，Native/Host 对应字段是否存在、实际质量如何、是否能比较，都尚未由本路线图确认。

| 优先级/方向 | 为什么值得检查 | 必须核实的测量与合法反例 |
|---|---|---|
| P1 资源能力关系 | R07 resource-pair及部分工具默认攻击有补检信号 | Web硬件并发/内存是否有同会话Native参照；逻辑核/可用核、量化/限额/单位不能机械相等 |
| P1 UA/platform关系扩展 | V1已有跨层/经验替代关系，适合厘清实际增量 | Native锚点是否恒定；UA合法自定义、类别解析、默认/当前快照的真实来源 |
| P1 语言/列表信息 | R07有语言长度和MIME数量线索，当前表示可能只保留粗统计 | 成员/顺序是否可用；应用与系统locale可合法不同；插件没有合适Native镜像时不强造跨层关系 |
| P2 屏幕/窗口几何 | screen配置仍漏检 | px/dp/CSS px、DPR、方向、窗口/全屏、缩放和采集时序；检查残差而非粗暴等值 |
| P2 时区关系 | timezone配置仍漏检 | 同一时刻、符号、分钟单位、DST、用户/应用设置；必须真实存在第二来源 |
| P2 GPU/WebGL关系 | V1 GPU候选经常不可用，WebGL配置漏检 | Native设备信息是否能映射渲染族；软件回退、ANGLE和驱动变化；不以“字符串不同=攻击” |
| P2 自动化表面 | webdriver/plugins/MIME配置有明显研究价值 | 独立参照可能不存在；保留单表面诊断/联合模型，不能拼一个恒真Native条件后称跨层 |

A 阶段仅定位与核实；B 阶段每个开发批次优先实现 **不超过3个具有证据的关系家族**。其他方向登记为待办或 `BLOCKED_MISSING_REFERENCE`，不阻塞整个批次。

### 6.2 模板而不是逐工具手工白名单

允许的起点包括：有语义的类别一致/不一致、经单位转换的单调边界、差值或比例残差、有限集合关系、同层与跨层条件的有界组合。每个候选记录字段来源、变换、单位、适用域、参数来源、合法反例和生成依据。

阈值/容差来自官方定义时记录确切出处；来自数据时只在训练折拟合。阈值候选可比较训练分位点与训练可用值之间的有限切点，但不得从全部开发样本生成同一组测试驱动阈值后声称折内学习。

不学习原始UA整串、配置特有字符串、唯一ID或任意高基数词表来记住工具。需要字符串内容时先定义低维可解释类别或关系语法，标明其开发来源；外部资料只使用与该字段相关的一手来源，避免扩大成无界文献审计。

### 6.3 不可观测或一致伪装必须允许成为研究边界

若所有允许观测表面被一致改写，或攻击发生在本系统没有采集的表面，当前会话规则可能没有可区分证据。不要为每个工具强造一条规则。应记录需要新增传感器/参照还是属于不可识别边界，并在最终主张中说明。

---

## 7. 学习与状态语义：先保持可比，再做有限敏感性

### 7.1 第一批使用 V1 的共同设置

为隔离表示变化，A 阶段的 `S_FLAT/J0` 先使用 V1 `GREEDY_OR` 数学语义和以下设置：[SEARCH]

- 整体训练clean报警预算 `alpha=0.05`；按 `floor(alpha*N_train_clean)` 计数。
- 目标 `MacroTPR_config_environment - 0.005 * complexity`；复杂度为子句数加文字出现次数。
- 单文字 OR；最多6个子句、复杂度最多12；训练attack/pre/post各自决策覆盖至少0.8。
- 可用支持至少3组三态、正例支持至少2组三态；不要求每条规则跨越多种攻击配置。
- 单表面数值阈值采用训练折0.25/0.5/0.75分位点；种子和并列规则沿用已保存合同。

这些限制是**第一批可比设置**，不是 V2 永久白名单。若联合表示出现超出当前实现的结构错误，先解决最小适配；不能静默改变目标或支持度来“跑通”。

### 7.2 B 阶段允许改变的内容

在每批运行前以机器可读配置登记少量对照，例如：

1. 原表示与新表示，保持学习器和预算不变；
2. 相同表示的贪心与有限 IP OR；
3. 存在训练侧组合需求时再比较有界 DNF2；
4. 仅在诊断支持时比较阈值候选、支持度或复杂度的一项变化。

不同时全排列全部算法、三个预算、多种阈值、多个支持度和所有表示。每批默认最多4个新模型规格，主要保留 `alpha=0.05`；0/0.10只作为明确的敏感性条件，不凭外层最高数值取代主操作点。

### 7.3 T/F/U、空模型和失败

初始V2继续保留：U的否定仍U；F OR U为U；T OR U为T；执行失败不变成缺失或正常。U不能成为学习器美化误报的渠道。

如有必要研究“已观察冲突触发、其他情况只输出未触发并附覆盖”的另一种策略，应另命名、另版本、与严格三值策略并列，不能只在一方使用。任何此类改变均不能把证据不足称为安全。本路线图不默认授权替换三值合同。

先检查支持/可用性门控是否混入强攻击归因前提，再决定是否修订。只有经证据确认是测量不必要的条件才调整，不统一放宽所有未知条件。

### 7.4 列生成不是当前阻塞项

先扩展有信息的原子并验证组合需求。仅在有限组合空间确有瓶颈、实验收益值得且获授权时，才实现 restricted master LP、对偶、pricing、reduced cost、迭代终止和整数恢复。

只做固定候选筛选、有限IP或启发式生成子句，均不叫复现列生成；改用本项目的集合报警预算与T/F/U后，也不能直接继承原论文优化保证。文献备忘见第16节。[DASH][MAIN9]

---

## 8. 改进是否真实：收益解释必须比总召回多一步

每次比较都输出：新增检出ID、丢失检出ID、clean新增报警ID、弃判变化、配置/环境分层、选中规则及来源。不能只展示增加的正例，也不能要求保住某一批已知24个ID而把它们变成训练特例。

诊断至少区分：

- **新增表示信号**：新原子在原表示不能区分的样本中提供差异。
- **不同选择偏好**：原池已经有信号，仅目标/搜索改变选择。
- **语义修订效应**：修改了可评估域或状态合同，收益不能全记到新规则。
- **采集/标签修订效应**：不能与纯模型升级混在同一主比较中。
- **开发集适配**：增益依赖某设备或工具默认值，需要额外验证。

对联合模型，应同时保留 `S_FLAT` 和 `CX` 对照。仅比较弱单层与强联合模型不足以说明跨层独特贡献。最终来源重训练也要在相同表示和选择流程下进行，公共语义门控与新研究原子来源分开记录。

---

## 9. 数据角色和验证：允许迭代，但不重新命名盲测

### 9.1 三个数据角色

| 数据角色 | 允许做什么 | 不允许声称什么 |
|---|---|---|
| `V1_EXPOSED_DEVELOPMENT` | 读已有错误、探索字段、提出关系、开发训练与诊断 | 不称从未影响设计的独立测试 |
| `INTERNAL_GROUPED_EVALUATION` | 使用继承的LOEO/LOCO比较开发版本，每折在train拟合 | 多轮看结果后不能据此宣称无选择偏差的最终泛化估计 |
| `INDEPENDENT_CONFIRMATION` | 模型和评价合同冻结后开放，评价未参与选择的材料 | 不先看结果改规则，再把同批材料叫最终确认 |

前两类可以引用同一历史材料的不同使用范围，但明确其暴露关系。重新切分不会消除历史暴露。模型选择和反复优化评价准则本身也可能过拟合。[CAWLEY]

### 9.2 分组策略

A/B的可比开发评价先沿用三折LOEO；LOCO用于选定候选的迁移诊断，不对每个小改动都跑全部14折。三态不拆分，继承已核实关联边界。

有足够组时，超参数/数据依赖模板选择在内层完成；当前仅三个环境组，不强行制造形式上嵌套但无足够支持的划分。采用固定批次参数与清晰的回顾性标签，先做有用开发实验。依赖全量开发反馈选择的最终版本仍需独立确认，嵌套切分不能抹去此前的人工设计反馈。

### 9.3 正常侧要覆盖合法变化，而不只是默认前后状态

**[V2采集设计，尚无对应新数据]** 后续独立材料应有：普通无干预时间漂移、正常应用/浏览器变化、合法UA自定义、语言/时区设置变化、设备资源差异、窗口/旋转/缩放，以及在研究范围内的渲染回退等。逐项确认这些行为是否属于正常范围；不要预设所有自动化都是攻击，也不要把合法场景事后移出clean。

现有UNKNOWN对照不因需要更多负例而晋级。新增对照和新工具执行须用户另外授权，不能由开发脚本自动启动。缺新数据时可以交付开发结果和采集规格，不阻塞A/B。

### 9.4 独立确认设计优先减少混杂

候选方法定型前制定确认材料计划：明确定义设备/环境、应用/浏览器版本、配置/机制、采集批次和操作人员记录；让主要攻击配置与多个环境交叉，而不是某配置只出现在某设备。

攻击工具“数量多”不等于机制覆盖充分。需要检验未见机制时，机制标签依据独立执行行为/实现证据定义，不直接拿工具名或字段集合充当机制。

确认样本量围绕希望区分的召回差异、clean误报范围、独立组数和采集预算设计；不把“多重复几百个阶段”当几百台设备。没有确定参数时不在本路线图虚报所需样本数或保证显著性。

---

## 10. 指标、选择和开发成功标准

### 10.1 每个规格都要有同一张结果表

至少保存：attack TPR微平均、配置/环境等权宏平均、pre报警、post报警、决定条件下clean报警、决策覆盖、弃判、失败、精确FTF、模型复杂度、候选与被选规则可用性、各配置和各折分母。

同一个数据阶段出现在多方法/多轨/多次运行中仍是同一个阶段。报告原始k/n，0次报警不表示总体FPR=0。样本或独立组过少时不生成虚假精确的显著性结论。

### 10.2 优先级与判据

开发期优先在相似clean报警和覆盖下提高**配置宏平均及多配置覆盖**，同时展示阶段微平均；不能靠数量较多的默认配置推动总体数字。复杂度作为权衡，而不是要求联合模型和一条规则具有相同结构。

“值得推进”的证据包括：在多个相关开发折中有可追溯的新配置覆盖；相同预算下形成更好的Pareto权衡；对合法变化更稳健；或者明确解决一种测量不可用瓶颈。单个开发折偶然增加3个阶段只能是初步线索。

工程验收不要求TPR达到80%、必须零报警、联合必须胜出或必须选满若干条。科学负结果也能关闭假设。若效果只来自单表面条件，应如实调整贡献陈述，而不是继续给跨层“补票”。

### 10.3 预期不是承诺

36/54的历史检出并集不是V2目标下限、上限或保证。V2可能优于、等于或低于该描述数字。真正的目标是一个可说明增益来源、在明确正常范围内误报受控、可以接受独立验证的方法。

---

## 11. 最小开发实验矩阵

### A 批次：马上得到信息，不再停在计划

| 实验 | 操作 | 新fit上限 | 身份 |
|---|---|---:|---|
| A0 基线/漏检诊断 | 复用V1结果、核实字段和谓词、全14配置原因表、固定表示冲突组 | 0 | 开发诊断 |
| A1 事后组合 | 对同折同ID的C0/W0保存结果做显式OR，包含全部clean/U/失败 | 0 | `POST_HOC_ENSEMBLE_DIAGNOSTIC` |
| A2 平铺多表面 | `S_FLAT`，三折LOEO，GREEDY_OR/OP05 | 3 | 回顾性开发评价 |
| A3 原关系联合 | `J0=S_FLAT∪C0`，相同三折及算法参数 | 3 | 回顾性开发评价 |

A2/A3 首先复用已声明单表面字段与编码语义，不引入任意字符串、高基数ID、新表面或新阈值语法。A批次最多6次实际fit；报错修正后的实际重试也占预算并保留，不计为“免费”。

A0/A1不应重新调用完整旧检测器。A2/A3需要在V2中做最小平铺/联合适配；如果接口确实不可用，报告最小技术阻塞并完成能完成的分析，不用伪造模型或重建整个V1平台绕过。

### B 批次：每次只回答一个主要问题

依据A结果选择不超过3个关系家族，实施 `CX`；先比较C0与CX，再比较S_FLAT/J0/JX。最多4个新规格，最多12个LOEO拟合为一轮默认批量。多个批次在同一获授权开发阶段内可连续进行，阶段总上限默认60个fit/3600秒算法与运行预算，且不得超出实际剩余研究额度。

如诊断表明瓶颈不在新关系，则选择有针对性的阈值/支持度或同池优化对照，不强迫新增关系。候选组合溢出应采用事前有界语法和训练侧生成，不能按全数据结果挑4096个最好子句再当全池。

B阶段结束时选择一个主候选和至多一个重要备选，不对每个开发规格重跑全部来源、单层、14折迁移和计时审计。正式来源/表示/迁移矩阵在C开始前一次确定。

### C 批次：定型、确认及论文级证据

保存最终候选语法、参数和学习程序；在授权范围内完成有必要的消融和迁移。若有独立材料，在确认集访问前保存最终开发集拟合模型和具体评估合同。

没有新材料时，交付 `DEVELOPMENT_COMPLETE_CONFIRMATION_PENDING`、当前证据、采集/确认计划；不得无限等待，也不得宣称最终确认已完成。R10论文级图表在此阶段之后恢复，或在方法稳定时与结果稿同步整理。

---

## 12. 只有三个检查点的路线

| 检查点 | 交付重点 | 外部审查的问题 | 停止位置 |
|---|---|---|---|
| **V2-A：诊断与首轮开发试验** | 14配置诊断、参照可用性、初始试验6fit以内、下一批假设 | 是否定位瓶颈？现有联合表示能否提供收益？先研究哪些关系？ | A批次完成后停一次 |
| **V2-B：有限方法迭代** | 新表示/必要学习变化、逐批实验台账、主候选与负结果 | 改进是否真实、可解释且非单纯环境捷径？ | 已授权批次内连续开发，形成候选或用尽预算后停 |
| **V2-C：定型与确认** | 最终合同、必要对照、独立确认或明确缺口、论文证据 | 哪些主张可以成立？哪些只能限定为开发发现？ | 当前版本收束，不自动开启V3 |

阶段内无需每个脚本、每个单元测试、每次正常开发修改都等待验收。阶段授权必须包含代码范围、数据角色、批次预算和停止点；发生标签裁决、新采集、未授权服务或显著扩容时才提前停。

**禁止形成新的“V2-A-1-R1-R2…”仪式链。**小工程错误直接在同一开发批次修复并保留尝试；只有科学合同实质改变才递增规格版本。一个负结果不要求重做所有上游。

---

## 13. 代码、产物与权限

### 13.1 路径建议

```text
V2_DEVELOPMENT_ROADMAP.md
deliverables/v2_development/
  EXECUTION_PLAN.md
  EXECUTION_STATUS.json
  DEFAULT_DEV_CONTRACT.json
  AGENT_START_PROMPT.md
  SOURCE_REGISTER.json
  research_references.bib

# 以下是拟新增实现/结果路径，不是当前资料包已实现的程序
hybridguard_agent/research/rule_learning_v2/
hybridguard_agent/config/rule_learning_v2_20260925/
hybridguard_agent/artifacts/discriminative_rule_learning_v2_20260925/
  A_diagnosis_pilot/
  B_development/
  C_confirmation/
```

V1源码和冻结目录只读；优先复用数学算法、固定转换和验证函数，以新适配层传入train-only数据与V2模型身份。不得为了使用旧入口，把真实数据改成synthetic或篡改旧授权/模型JSON。

若旧平台的正式入口过度绑定V1作业，建立小型、显式V2训练入口，记录代码/配置/成员摘要并复用验证过的内核；不重写完整dispatcher/权限平台。该入口输出V2模型，不冒称由V1冻结任务产生。

### 13.2 每批最小产物

`batch_spec.json`（运行前），`trials.jsonl`，模型和必要预测/解释，`metrics.csv`，`candidate_changes.jsonl`，`REPORT.md`。数据可用性和失败表可以合并进报告配套JSON，不要求每项再造一套独立验收文件。

每条试验记录规格、代码版本、候选/测量版本、训练与评价角色、fold、模型ID、参数、种子、起止时点和结果路径；覆盖原模型/原结果是禁止的。小试验保存足够复核的信息，不反复复制完整历史数据和运行时。

### 13.3 开发和确认的重试策略不同

开发期允许修复确定的工程错误后重跑，但必须保留失败尝试、原因和新代码版本，实际次数计入预算。禁止自动挑最好随机种子、抹掉不利有效结果或把无模型当低误报。

确认期的失败处理和技术重试在看到确认标签前预先规定；科学负结果不触发重跑选择。两种场景不得用同一条“永不重试”或“随意覆盖”规则混管。

### 13.4 预算不成为重建工作的理由

V1原账本和R09关联非拟合账目保留只读。V2建小型关联开发账本，启动时计算剩余额度；不得通过新目录把已用71次fit归零。未获扩容授权时，V1原限额及实际剩余额度是硬上限。[R09]

A批次额外上限为6次fit、600秒实际训练/试验执行计费，取其与剩余额度较小者；这是计算预算，不是承诺完成时间。B批次预算需下一次授权，默认总上限如第11节。文档编写本身不启动计算或获得永久fit授权。

---

## 14. 开发图表与未来论文材料

开发阶段保留四类可读图表即可，不制作完整R10版式：

1. 配置×表示的检出、clean报警、弃判矩阵，全部配置固定排序。
2. 新增/丢失检出与表示覆盖的对应关系；集合并集标事后描述。
3. 模型宏/微检出—clean报警—覆盖—复杂度权衡，显示k/n和开发身份。
4. 规则三态响应及环境/合法变化失败案例；未选原子与实际触发解释分开。

固定源表可以跨版本复用。最终方法稳定后再组织Fig.1–6、claim ledger和可公开复现包。确认缺失、人工未审查、未运行分支用明确状态，不画人造性能。

---

## 15. 值得争取的论文主张与不可提前承诺的内容

**拟研究表述：** 基于官方语义和观测数据构造具有来源与有效性状态的关系，通过受控三态开发数据学习简洁判别集合；比较跨层、单表面与平铺/联合表示，并检验新关系是否改善干预覆盖及合法变化下的误报权衡。

能够形成贡献的可能内容包括：被验证的新跨层表示、明确的可观测性边界、减少绝对阈值捷径的关系设计、互补规则集合、正确的来源与表示分解，以及有意义的独立确认。不是把通用规则学习算法应用一次就宣称算法创新。

不得提前承诺：整体80%/90%检出、总体零误报、所有工具/未知机制泛化、所有跨层方法优于单层、官方与经验固定百分比分工，或系统已能证明恶意意图。

---

## 16. 核心文献备忘

### 16.1 Boolean Decision Rules via Column Generation

- 作者：Sanjeeb Dash、Oktay Günlük、Dennis Wei；发表时署名IBM Research。
- 发表：*Advances in Neural Information Processing Systems 31*，NeurIPS 2018。
- 引用键：`dash2018boolean`；预印本标识 `arXiv:1805.09901`。
- 一手入口：[NeurIPS正式页面][DASH]、[IBM作者机构页面][IBM]、[arXiv记录][ARXIV]；文献核对日期2026-09-25。

**[文献内容]** 论文学习DNF（OR-of-ANDs）或CNF（AND-of-ORs）规则，以整数规划权衡分类与简洁性，使用列生成搜索规模很大的子句空间。训练优化界不等于测试泛化或安全检测保证。[DASH][IBM]

**[继承的阅读备忘]** V1主线第9节已经记录原文Hamming loss与0–1错误不同、受限主问题和定价流程，以及相关章节位置。V2若真正实现CG，必须回到正式论文核对目标、定价和保证条件；不能把旧摘要当完整实现规范。正式版与预印本的实验数字不混用。[MAIN9]

**[V2设计]** 初期复用固定池OR/有限IP。若扩展到本项目的三值原子、集合clean预算、分组权重或新关系生成，应明确其为改编设计，并验证自己的数学与代码合同，不直接声称继承原论文保证。

### 16.2 模型选择偏差

Gavin C. Cawley 与 Nicola L. C. Talbot，*On Over-fitting in Model Selection and Subsequent Selection Bias in Performance Evaluation*，JMLR 11，2079–2107，2010，引用键 `cawley2010overfitting`。[CAWLEY]

**[文献内容]** 反复优化有限样本上的模型选择准则可能产生选择过拟合和乐观性能估计。**[本项目应用]** 这不是禁止开发迭代，而是要求将开发选择与最终确认分开，并披露历史暴露。已有材料可以立即用于A/B，不必每次修改都收新数据。[CAWLEY]

---

## 17. V2 到哪里可以结束

当A/B得到一个有依据的候选方法，C完成预定评价或明确记录独立确认缺口，并能对以下问题给出可追溯答案，即可关闭当前V2开发周期：

- 相比V1改了哪些表示/学习条件，收益和损失分别来自哪里？
- 与有竞争力的单表面/平铺表示相比，跨层关系提供了什么？
- 正常侧适用范围是什么，剩余攻击为何漏检？
- 哪些结果只是开发发现，哪些已有独立支持？
- 哪些假设被否定，哪些分支不值得继续投入？

达不到理想TPR不意味着流程无限延长。若有限开发预算用尽而无实质增益，交付负结果、最有价值的缺口和下一项明确决策；不自动再造V3。R10在方法定型后恢复，不是V2启动的门槛。

---

## 证据与参考入口

所有仓库事实以本文顶部提交为准。源表/报告定位清单见 [SOURCE_REGISTER.json](deliverables/v2_development/SOURCE_REGISTER.json)。仓库链接是路径定位；源文件中的“待验收/未推送”描述可能属于旧执行时点，不在本包中改写。

[MAIN]: RESEARCH_MAINLINE.md
[MAIN9]: RESEARCH_MAINLINE.md#9-重要参考文献备忘boolean-decision-rules-via-column-generation
[PLAN]: deliverables/formal_experiment_execution_plan/EXECUTION_PLAN.md
[S06]: hybridguard_agent/artifacts/formal_manipulation_v1_20260923/06_e1/STEP_REPORT.md
[R05]: hybridguard_agent/artifacts/discriminative_rule_learning_v1_20260924/R05_primary/STEP_REPORT.md
[R06]: hybridguard_agent/artifacts/discriminative_rule_learning_v1_20260924/R06_sources/STEP_REPORT.md
[R07]: hybridguard_agent/artifacts/discriminative_rule_learning_v1_20260924/R07_representation/STEP_REPORT.md
[R07PAIR]: hybridguard_agent/artifacts/discriminative_rule_learning_v1_20260924/R07_representation/paired_comparison.jsonl
[R07RULES]: hybridguard_agent/artifacts/discriminative_rule_learning_v1_20260924/R07_representation/MODEL_RULES.md
[R08]: hybridguard_agent/artifacts/discriminative_rule_learning_v1_20260924/R08_validation/STEP_REPORT.md
[R09]: hybridguard_agent/artifacts/discriminative_rule_learning_v1_20260924/R09_cost_explanation/STEP_REPORT.md
[GRAMMAR]: hybridguard_agent/config/rule_learning_v1_20260924/candidate_grammar.json
[SEARCH]: hybridguard_agent/config/rule_learning_v1_20260924/learning_search_space.json
[DASH]: https://papers.neurips.cc/paper_files/paper/2018/hash/743394beff4b1282ba735e5e3723ed74-Abstract.html
[IBM]: https://research.ibm.com/publications/boolean-decision-rules-via-column-generation
[ARXIV]: https://arxiv.org/abs/1805.09901
[CAWLEY]: https://www.jmlr.org/papers/v11/cawley10a.html

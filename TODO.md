# TODO

## 2026-09-22：MTC 离线研究

- [x] 用户取消采购对账，以 MTC 实际统计为准。
- [x] 本机后端和 ngrok 已退役；P0 最终关闭批次副本已保存，不再启动本机采集链。
- [x] P1：v9／v11 快照、回执关联、缺失状态与字段质量、数值比较 v2 已完成；见 `deliverables/mtc_p1_20260922/P1_REPORT.md`。
- [x] P2：冻结分组／切分、代表记录、任务准入和事实标签标准；见 `deliverables/mtc_p2_20260922/P2_REPORT.md`。
- [x] P3：冻结旧规则基线，完成 45 项有限候选发现／开发检查；v2 研究目录保留 10 经验关系、14 语义约束、6 采集器自洽项，15 项未晋级；完整反例保留，44 项测试通过。权威结果为 `mtc_p3_discovery_20260922_r2`，见 `deliverables/mtc_p3_20260922/P3_REPORT.md`。保留验证集继续锁定，未报告检测效果。
- [ ] P4：适配 paired244 证据／运行时，落实旧 provider 版本关系和低传感器阈值的处置；研究关系不直接等同攻击规则，保留 unknown／not_applicable 与证据依赖。

以下为历史采集过程记录。

## 百度 MTC 批量采集前

- [x] **采集网页自动更新两端配对结果，并提供统一完成提示。**
  - 状态：2026-09-16 已实现并通过本地验证，2026-09-17 已发布后端和网页、生成提测 APK；MTC 真机联调待执行。对外《百度MTC采集操作流程说明》与实现一致。
  - 浏览器先上传成功、后台仍等待 App 时，网页继续查询本轮配对状态；仅在 App 与 Browser 两端数据均已上传、后台确认本轮 `pair_status=completed` 后显示“本机采集完成”。
  - 由我方完成网页与后台的状态查询对接，确保状态属于当前采集轮次；MTC 只读取页面提示，无需另行接入后台查询。
  - 采集失败或配对超时分别显示“采集失败：原因”／“配对超时：原因”；不能将浏览器单端上传成功或固定等待结束视为整轮成功。
  - 配对等待期间自动刷新；成功、失败或超时后停止查询。统一替换旧的 `Browser collection complete.` 成功提示，避免出现两套完成标准。
  - 等待策略：每 2 秒查询一次，最长等待配对 120 秒；连续 3 次查询异常或不可恢复的接口错误时显示失败并停止查询。
  - 本地验收：前端 19 项测试、后台配对 13 项测试及站点构建通过；真实 Chrome 页面连接隔离后台，两种上传顺序均显示“本机采集完成”，完成后停止查询、无页面脚本异常。App 未上报、查询异常、配对超时、回包轮次不匹配及迟到成功回包均有边界测试。
  - 参考位置：`browser_probe_site/public/browser-probe-adapter.js`、`browser_probe_site/public/browser-probe.html`、`backend_server/main.py`。

- [x] 2026-09-17 已启动新版后端并通过受限 ngrok 隧道公开采集接口，已发布新版静态网页；公网就绪检查、配对接口鉴权、非采集路径拦截及浏览器跨域预检通过。
- [x] 2026-09-17 已将 ngrok 地址写入 App 构建配置并生成 v9 提测 APK；26 项 App 单元测试通过，安装包内采集、票据与轮询地址均已核对。
- [ ] 在 MTC Android 真机上确认采集全过程与最终提示识别。
- [x] 2026-09-17 已隔离本次数据目录 `backend_server/collection_runs/mtc_20260917/`，后端已重启并经公网确认就绪；旧文件中的 1 条网页自测记录已单独归档，旧数据已恢复。存储隔离、批次和配对共 20 项测试通过。

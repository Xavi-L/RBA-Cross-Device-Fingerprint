# HybridGuard Agent 项目简报

> 用途：供导师确认后续研究方向、项目贡献与论文大纲
> 更新：2026-07-22

## 1. 范围与定位

本简报讨论的是 HybridGuard 在原本科毕设之后的研究演进与后续方向。原毕设已经完成 Android Native、WebView 宿主和 Web Runtime 三端采集、`session_id` 会话对齐、FastAPI 接收以及端侧轻量评分等基础能力；这些内容在后续项目中作为可运行的实验基座，不再是论文要重点重复论证的主要贡献。

后续项目的目标，是把 HybridGuard 建设为一个**可溯源、可验证的三端跨层设备完整性 Agent**：系统从 Android、WebView、Chromium 等官方资料中提取字段语义和跨层不变量，从攻击工具的文档、源码和受控实验中提取攻击机制、可观察影响与对应防御机制，再由 Agent 对具体设备会话进行字段感知检索、分组推理和结果验证。

主任务应定义为：

> **三端跨层设备完整性与受控环境操纵检测。**

在没有真实欺诈、账户损失或拒付标签前，不把项目直接定义为“欺诈检测”。

本简报仍不包含 `hybridguard-browser-fingerprint-research/` 独立仓库的代码、数据或统计结果。文中“攻击工具知识提取”指 HybridGuard Agent 后续需要建立的通用研究能力，而不是引用该独立仓库的现有成果。

## 2. 建议论文题目

建议主标题为：

> **HybridGuard：面向移动端三端设备完整性分析的可溯源可验证智能体框架**

可选的英文表述为：

> **HybridGuard: A Provenance-Aware and Verifiable Agent for Cross-Layer Mobile Device Integrity**

如果希望在题目中直接体现三项贡献，也可以采用：

> **基于三端对齐、攻防知识与开放数据集的移动设备完整性智能体研究**

## 3. 核心研究问题

后续研究不再以“继续扩字段、训练一个更强分类器”为中心，而是回答以下问题：

1. 如何把 Android Native、WebView Host 和 Web Runtime 的异构字段规范化，并显式表示三端之间应当成立的对应关系？
2. 如何从官方文档中提取字段语义、合法取值和容错边界，从攻击工具中提取攻击原理、操纵位置、可观察痕迹及对应防御机制？
3. 如何保证 LLM 生成的候选规则不是幻觉或数据捷径，而是经过来源引用、程序验证、反例检查、配对攻击验证和人工审核后才能进入正式知识库？
4. 如何让 Agent 只检索与当前字段和证据组相关的知识，并验证其引用、规则触发条件和最终结论？
5. 如何构建一个同时包含三端原始信号、跨层证据、攻击来源、配对关系和稳定画像分组的开放数据集，用于评估未见设备和未见攻击工具上的泛化能力？

## 4. 总体技术路线

项目由“离线知识构建、在线 Agent 推理、开放数据集建设”三条相互连接的链路组成。

```text
离线知识构建
Android / WebView / Chromium 官方文档 ─┐
攻击工具文档、源码与受控实验 ───────────┼─> 来源登记与结构化抽取
训练折内的案例和候选模式 ───────────────┘          │
                                                     ├─> 字段语义与跨层不变量
                                                     ├─> 攻击技术与防御机制卡片
                                                     └─> 候选规则
                                                          │
                           程序校验 + 反例检查 + 配对攻击验证 + 人工审核
                                                          │
                                            版本化知识库与检索索引

在线 Agent 推理
177 字段三端会话
  -> Schema 校验与规范化
  -> 三端跨层 EvidenceBundle
  -> 字段感知知识检索
  -> LLM 分组语义推理
  -> Verifier 检查字段、规则、引用与结论
  -> 风险判断、建议动作与完整 DecisionTrace

开放数据集
clean_pre -> attack -> clean_post 配对采集
  -> 来源与攻击事实 Manifest
  -> Schema / QC / stable profile 分组
  -> 隐私审查、版本冻结与公开 split
  -> 三端设备完整性与受控操纵开放基准
```

LLM 在其中负责提出候选知识和完成语义判断，但不能直接把规则写入正式知识库，也不能单独决定攻击真值。数据挖掘只用于发现候选模式，不是知识来源和最终贡献的中心。

## 5. 预期三项核心贡献

### 5.1 贡献一：三端对齐与跨层证据表示

这里的“三端对齐”不是简单把 177 个字段拼接成一个向量，而是建立 Android Native、WebView 宿主与 Web Runtime 之间可计算、可解释的关系，例如：

- Android 版本、设备型号与 Web/User-Agent 暴露信息是否一致；
- Native 物理屏幕、Web 逻辑屏幕和 DPR 是否在容错范围内对应；
- WebView provider、Chromium 主版本和 Web Runtime 能力是否匹配；
- App 宿主、JSBridge、安装来源和页面运行环境是否形成完整链路；
- Native GPU/硬件、WebGL Renderer、传感器和运行时表现是否相互支持；
- 字段缺失、权限拒绝、API 不支持和真实异常是否被明确区分。

最终产物不是一份只适用于某个模型的特征表，而是稳定的 Schema、canonical field registry、跨层 predicate 和结构化 EvidenceBundle。它们既供规则引擎使用，也供 Retriever、Reasoner 和 Verifier 共同使用。

### 5.2 贡献二：从多源知识中构建可验证的防御机制

防御知识分为两类主要来源：

1. **官方语义知识**：从 Android、WebView、Chromium、Web API 及相关标准文档中提取字段定义、版本约束、安全语义、合法例外和容错边界。官方文档用于说明“字段是什么意思、什么关系应成立”，不直接提供风险概率或项目阈值。
2. **攻击机制知识**：从攻击工具文档、公开源码、技术报告和受控运行记录中提取攻击目标、前置条件、操纵层、影响字段、跨层破坏方式、可观察痕迹、失效条件和对应防御检查。

知识进入正式系统前采用可审计的晋级流程：

```text
可追溯来源
  -> 结构化知识卡 / 攻击技术卡 / 防御机制卡
  -> LLM 提出 CandidateRule
  -> 确定性统计与 predicate 测试
  -> benign 反例与容错检查
  -> clean/attack/post 配对验证
  -> 人工审核
  -> 版本化正式知识库
```

在线阶段，Agent 根据当前异常字段和证据组检索相关知识，生成带来源引用的推理结果；Verifier 再检查被引用字段是否存在、规则触发条件是否满足、官方知识是否被误写成风险阈值，以及结论是否得到证据支持。这样形成“知识提取—规则审核—检索推理—结果验证”的防御闭环。

### 5.3 贡献三：可复现的三端攻防开放数据集

开放数据集不只发布若干 JSONL 会话，而应同时发布可复现实验所需的上下文：

- Android Native、WebView Host、Web Runtime 三端原始信号及字段状态；
- 规范化后的字段路径和跨层 EvidenceBundle；
- 数据来源、采集版本、provider、App 构建和时间信息；
- 同一稳定设备上的 `clean_pre -> attack -> clean_post` 配对关系；
- 攻击工具、配置、执行是否成功、影响了哪些层和哪些字段的事实标签；
- 对应的攻击技术卡、防御机制卡和可引用来源；
- `stable_device_key`、pair group 和固定的训练/开发/测试 split；
- 隐私脱敏、许可证、版本号、数据卡和已知局限。

数据规模同时报告 session 数、stable profile 数、攻击工具/家族数和完整配对数，不能把重复会话当成独立设备。正式评估应包含未见设备、未见攻击工具、未见攻击家族、未见 provider 和时间外推等 OOD 设置。

## 6. 毕设之后已经完成的工作

原毕设基座之外，当前仓库已经为 Agent 方向推进了以下工作：

| 工作 | 当前产物 | 在后续研究中的作用 |
| --- | --- | --- |
| 扩展三端采集协议 | `:featureapp` 的 177 字段采集、字段状态、collection manifest、诊断、超时和重试机制 | 为统一 Schema、三端对齐和可追溯数据集提供输入。 |
| 官方知识收集 | `google_official_kb/` 中的官方来源元数据、字段风险卡和合入报告 | 已验证“来源—字段—规则”可审计知识管线的基本形式。 |
| 数据治理与证据管线 | `hybridguard_agent/` 中的 Schema、SampleManifest、稳定画像、QC、知识版本和 EvidenceBundle 构建脚本 | 将原始会话转换为 Agent 可消费且不泄漏标签的结构化证据。 |
| Agent/RAG 设计 | `hybridguard_agent_rag_guide/` 中的研究边界、架构契约、攻击采集、知识晋级、Verifier、实验和论文路线 | 已形成从数据到论文的系统化设计，后续按阶段落地。 |
| 初步验证工具 | 官方知识消融、分组证据融合和 grouped CV 相关实验 | 作为方法开发和基线，不作为最终攻击检测结论。 |

因此，旧的 Android App、FastAPI 和随机森林闭环只需要在论文中简要说明为系统基础；后续正文应重点讨论上表中的数据协议、知识构建、Agent 推理和开放基准。

## 7. 后续实施重点

### 阶段一：完成多源知识接入

- 建立 Android、WebView、Chromium、Web 标准等官方来源的版本化抓取与结构化抽取流程；
- 建立攻击工具知识接入模板，统一记录工具版本、攻击目标、操纵位置、成功条件、观察字段和防御检查；
- 将官方语义、确定性不变量、攻击机制、经验候选规则和案例知识分层保存；
- 为每条知识保留来源、版本、适用字段、证据强度和适用边界。

### 阶段二：实现可验证 Agent

- 由确定性程序生成三端 EvidenceBundle，避免让 LLM 直接比较所有原始字段；
- 实现字段感知的 Exact/BM25/Dense/Hybrid 检索基线；
- 实现分组 Reasoner，对三端一致性、宿主完整性、物理运行环境和攻击场景分别推理；
- 实现 Verifier，拦截不存在字段引用、无效规则触发、错误官方结论和证据不足的风险判断；
- 保存完整 DecisionTrace，使每次结果能够复核和重放。

### 阶段三：建设并发布开放数据集

- 按同一 stable profile 采集 clean/attack/post 配对样本；
- 冻结 SampleManifest、Attack Manifest、Schema、知识库和 split manifest；
- 优先扩大独立稳定画像和攻击家族覆盖，而不是只增加重复 session；
- 设计公开层级，分离可公开特征、敏感标识和内部审计信息；
- 发布数据卡、采集协议、基准脚本、许可证和版本变更记录。

### 阶段四：完成研究评估

- 对比确定性规则、传统模型、直接 LLM、整库 Prompt、不同 Retriever、无 Verifier 与完整 Agent；
- 评估受控攻击召回、正常样本误报率、字段/规则引用正确率、invalid trigger、校准和系统成本；
- 通过 leave-one-device/tool/family/provider 和时间外推验证泛化；
- 使用错误检索、随机知识卡和 Oracle 检索等对照，确认收益是否真正来自正确知识和验证机制。

## 8. 建议论文大纲

1. **绪论**：移动 Hybrid App 的跨层设备完整性问题、研究动机、挑战与三项贡献。
2. **背景与威胁模型**：三端攻击面、受控环境操纵、知识增强 Agent 及结论边界。
3. **三端对齐与结构化证据表示**：177 字段 Schema、规范化、跨层 predicate、EvidenceBundle 和数据质量控制，对应贡献一。
4. **可溯源防御知识构建**：官方文档抽取、攻击工具机制抽取、知识分层、CandidateRule 晋级和版本治理，对应贡献二。
5. **字段感知检索与可验证 Agent 推理**：Retriever、分组 Reasoner、Verifier、外部融合和 DecisionTrace。
6. **开放数据集与基准协议**：配对采集、Attack Manifest、稳定画像分组、数据发布和 OOD split，对应贡献三。
7. **实验设计与结果分析**：主结果、知识来源消融、Retriever/Verifier 消融、未见设备与未见攻击泛化、效率和失败案例。
8. **相关工作、伦理与局限**：设备指纹、移动安全 Agent、攻击知识库、隐私和双重用途风险。
9. **结论与展望**：总结三项贡献及后续扩展。

## 9. 当前可以宣称与暂不能宣称的内容

当前可以说明：

- 已有可运行的三端采集基座和 177 字段扩展采集协议；
- 已建立官方知识收集、数据治理、EvidenceBundle 和 Agent/RAG 的初步框架；
- 已形成面向三端对齐、防御知识与开放数据集的明确研究路线。

当前暂不能说明：

- Agent 已经在真实攻击检测上达到确定准确率；
- RAG、攻击知识或 Verifier 已经带来稳定性能提升；
- 当前 session 数等于独立设备数；
- 数据归纳出的规则能够泛化到未知攻击家族；
- 在没有真实业务标签时，系统已经具备通用欺诈检测能力。

只有在完成带独立事实标签的配对攻击数据、锁定测试集和 OOD 实验后，才能进一步讨论未见设备和未见攻击工具上的泛化能力。

## 10. 供导师确认的事项

1. 是否认可将论文主线从“毕设系统实现”调整为“知识驱动、可溯源、可验证的三端设备完整性 Agent”？
2. 是否认可“三端对齐、防御机制、开放数据集”作为三项核心贡献？
3. 防御机制部分是否以“官方语义 + 攻击工具机制 + 受控验证 + 人工晋级”为主要方法，而不以数据挖掘结果作为知识真值？
4. 开放数据集是否作为与 Agent 方法同等重要的贡献，并优先补充独立 stable profile、攻击家族和 clean/attack/post 配对？
5. 论文题目与章节是否采用第 2、8 节给出的 Agent 方向表述？

## 11. 主要依据

- [Agent/RAG 工作索引](hybridguard_agent_rag_guide/README.md)
- [研究总览与结论边界](hybridguard_agent_rag_guide/01_OVERVIEW_AND_RESEARCH_BOUNDARIES.md)
- [论文写作、结论与风险治理](hybridguard_agent_rag_guide/09_PAPER_WRITING_CLAIMS_AND_RISKS.md)
- [当前 Agent 数据管线](hybridguard_agent/README.md)
- [Google 官方知识库](google_official_kb/README.md)
- [项目总览](README.md)

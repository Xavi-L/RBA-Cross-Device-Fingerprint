# HybridGuard 项目整体框架与预期成果说明

> **文档用途**：供导师整合两名学生的工作说明并设计论文整体框架。
>
> **写作口径**：本文按项目最终形成的完整系统描述，不区分阶段性实现进度。
>
> **工作范围**：本文只说明主仓库中的 HybridGuard 采集、配对、数据治理、证据建模、知识增强推理、决策验证与实验评价工作，不包含 `hybridguard-browser-fingerprint-research/` 中的浏览器指纹攻击研究、攻击工具实现、攻击环境构造或执行结果。完整实验所需的受控操纵样本，在本项目中仅作为带有标准 Manifest、配对关系和核验事实的外部输入接入。

## 一、项目概述

HybridGuard 面向移动端 Hybrid App 的无感风控与风险自适应认证（Risk-Based Authentication, RBA）场景，研究如何联合利用 Android Native、WebView 宿主、App 内 Web 运行时和独立系统浏览器中的设备指纹信息，对同一设备在不同运行层和不同容器中的环境表征进行对齐、互证和解释。

项目的核心任务不是直接判断真实业务欺诈，而是完成**三端跨层设备完整性与受控环境操纵检测**：系统先把多端原始指纹转化为确定、可复现的跨层证据，再结合官方语义知识、可执行规则和经审核的经验知识进行推理，最终输出风险分层、证据引用、置信信息、处置建议以及完整决策轨迹。该结果可作为登录认证、二次验证、限权或人工复核等策略的输入。

项目以商用真机云、移动测试平台和自有真机组成的大规模、多来源设备采集为数据基础，覆盖不同品牌、设备画像、Android 版本、WebView/浏览器版本、平台来源和采集时间。经过统一契约、来源审计、脱敏、稳定画像分组和版本冻结后，数据以开源数据集和标准化 Benchmark 的形式发布，为跨层设备完整性、知识检索、可验证推理和 OOD 泛化研究提供可复用的公共评测基础。

项目拟回答的核心问题包括：

1. 多端设备指纹能否在同一采集上下文中稳定配对，并形成比单端特征更完整的设备环境视图；
2. 显式的跨层一致性证据，能否比直接堆叠原始字段更有效地刻画环境异常和受控操纵；
3. 官方知识、确定性规则和训练内经验知识，能否提高推理的语义正确性、边界处理能力和可审计性；
4. 字段感知检索与结果验证器，能否减少大模型的无依据引用、错误规则触发和解释幻觉；
5. 在严格按稳定设备画像、配对关系和攻击场景分组后，系统结论能否保持跨设备、跨来源、跨配置和时间外推能力；
6. 如何把大规模、多来源设备观测转化为具有明确任务、固定划分、强基线和统一指标的开源数据集与 Benchmark。

## 二、整体技术框架

HybridGuard 由规模化采集与配对、服务与数据治理、开源数据集与 Benchmark、证据建模、知识构建与检索、Agent 推理与验证、风险融合与输出、实验评价八个部分构成。

```text
商用真机云 / 移动测试平台 / 自有真机
  ├── FeatureApp：Native 84 + WebView 26 + App 内 Web 67 = App177
  └── 独立系统浏览器：Browser67
                  │
                  ▼
来源校验、回执绑定、批次管理与 App/Browser 配对
  ├── paired244 正式主视图
  └── App-only 177 留存视图（Browser 缺失不填零）
                  │
                  ▼
Schema 校验、字段状态、规范化、QC、稳定画像分组与数据冻结
  ├── 开源数据集 + 固定划分 + Benchmark
  │     └── 任务、基线、评测器、数据卡与版本治理
  └── SampleManifest + EvidenceBundle
        └── 确定性跨层比较与结构化异常证据
                  │
                  ▼
核心规则执行 + 字段感知知识检索 + LLM 分组语义推理
                  │
                  ▼
字段、规则、引用、输出结构与结论一致性验证
                  │
                  ▼
外部融合与校准
                  │
                  ▼
RiskDecision + DecisionTrace + 认证处置建议
```

系统同时包含两条相互隔离的知识链路：

- **离线知识构建链路**：仅使用训练组数据和公开官方知识，由大模型提出候选规则，再经过字段检查、可执行谓词编译、支持度与反例统计、开发组验证、冲突检查、人工审核和单元测试后，发布版本化知识卡及检索索引。
- **风险推理链路**：只读取冻结的 Schema、字段注册表、知识库、检索索引、Prompt、模型和融合参数；运行过程中不修改正式知识库，也不接触标签、数据划分、攻击工具或来源捷径。

## 三、各层模块及职责

| 层级 | 主要职责 | 对应项目模块与产物 |
|---|---|---|
| 多端采集层 | 在同一移动设备上下文中采集 Native、WebView、App 内 Web 和独立浏览器信号，并记录逐字段可用状态 | `android_app/HybridGuard/featureapp/`、`web_probe/`、`browser_probe_site/` |
| 配对与服务层 | 接收异步上报，管理采集批次、回执和短期配对凭证，将 App 与 Browser 原始数据按来源证据绑定 | `backend_server/` 中的采集 API、receipt、batch、pair provenance 和原始数据 |
| 数据治理层 | 校验固定字段契约，区分特征值、字段状态与控制面元数据，生成 paired244、App-only、隔离记录、样本索引、QC 和冻结清单 | `hybridguard_agent/scripts/build_latest_paired244_snapshot.py`、Schema 与 artifacts |
| 开源数据集与 Benchmark 层 | 组织多平台批量采集、覆盖矩阵、公开数据版本、固定分组划分、任务定义、基线、统一评测和数据卡 | `device_cloud_catalog/`、`hybridguard_agent/`、实验与评测脚本 |
| 证据层 | 将高维原始字段转化为有限、可解释、可重复计算的派生事实、跨层比较、异常项和查询字段 | `SampleManifest`、`EvidenceBundle`、确定性比较器与规则执行器 |
| 知识与检索层 | 管理官方语义、确定性关系、经验规则和训练内案例，按字段、证据组、适用版本和容错条件检索 | `scoring/rule_knowledge_base.json`、`google_official_kb/`、`hybridguard_agent/retrieval/` |
| Agent 推理层 | 依据证据组组织结构化推理，输出分组判断、引用字段、引用规则、支持证据、容错证据和不确定性 | `hybridguard_agent/runtime/`、结构化 LLM 输出契约 |
| 验证与决策层 | 验证字段是否存在、规则是否满足、引用是否有效、强制规则是否执行以及结论是否受证据支持，再完成融合、校准和策略映射 | `hybridguard_agent/verification/`、`RiskDecision`、`DecisionTrace`、只读分析 API |
| 部署与业务接口层 | 为研究审计提供完整 Agent 分析，为低延迟和隐私敏感场景提供冻结的端侧轻量策略，只上传风险摘要 | `backend_server/agent_runtime_api.py`、`android_app/HybridGuard/riskapp/` |
| 实验评价层 | 执行分组切分、强基线比较、检索评价、模块消融、OOD 测试、统计检验和系统开销评价 | `llm_grouped_fusion_validation/`、`rf_grouped_fusion_validation/`、`ablation/`、实验 artifacts |

## 四、核心数据与证据设计

### 4.1 多端特征与正式数据单元

FeatureApp 形成固定的 App177：84 项 Android Native 特征、26 项 WebView 宿主特征和 67 项 App 内 Web 运行时特征。独立系统浏览器使用同一版本的 67 项 Web 探针形成 Browser67。只有 App 数据、Browser 数据、采集回执、已关闭批次和配对来源均通过校验时，二者才组成具有 244 项固定字段契约的正式配对记录 `paired244`。

若独立浏览器采集未完成，合法的 App177 进入 App-only 留存视图，Browser 字段不补造、不填零，也不把“缺失”解释成风险。每个字段同时保留 `observed`、`unsupported_by_os`、`permission_denied`、`runtime_error`、`timeout` 或 `not_applicable` 等状态，使“真实值为零”和“没有采到”在语义上严格分离。

### 4.2 数据控制面与防捷径原则

原始特征与来源控制面分开保存。控制面记录样本标识哈希、采集版本、原始载荷哈希、回执、批次、配对关系、稳定设备画像、标签事实和质量状态；模型和检索查询只接收脱敏后的特征证据，不接收 provider、攻击工具、pair role、fold、原始会话标识或其他可能泄漏答案的元数据。

正式数据冻结同时产生特征目录、Schema 版本、样本索引、选择审计、隔离记录、QC 汇总和构建 Manifest。所有论文结果均绑定明确的 run ID、数据版本、知识版本和模型版本，保证可以从冻结输入重新生成。

### 4.3 四类核心中间表示

1. **SampleManifest**：描述样本来源、采集协议、载荷哈希、稳定画像、配对关系、事实标签和质量状态，不作为模型特征。
2. **EvidenceBundle**：保存由程序确定性提取的派生事实、跨层比较、异常项、容错信息和检索字段；相同输入与提取器版本生成相同证据哈希。
3. **KnowledgeCard**：统一表达官方语义、确定性规则、经验规则和训练内案例，并记录来源、适用字段、反例、容错、版本、状态和适用时间。
4. **DecisionTrace**：保存一次决策使用的 Schema、规则库、检索索引、Prompt、模型、融合参数、检索结果、分组判断、引用、验证结果、延迟与成本。

### 4.4 数据规模的计数口径

| 口径 | 含义 | 不可替代的口径 |
|---|---|---|
| session | 一次采集会话或一条观测记录 | 独立设备数 |
| App177 record | 一次通过固定契约校验的 App 三层观测 | 物理设备数 |
| paired244 record | 一次来源完整的 App177 与 Browser67 配对观测 | 独立设备数或受控场景数 |
| App-only record | Browser 未完成但合法保留的 App177 | paired244 主任务样本 |
| stable profile/group | 一组被判定来自同一稳定设备或可信设备画像的会话 | 经核验的物理设备数 |
| scenario/pair group | 同一受控场景下相互关联的基线、操纵与恢复观测 | 独立设备数 |
| 177/244 fields | 固定 Schema 的字段数量 | 数值模型维度、样本数或设备数 |

因此，数据规模需要同时报告 session、App177、paired244、App-only、stable profile 和 scenario/pair group 等数量。重复采集能够增加运行状态观测，但不能被表述为增加了独立设备覆盖。

## 五、大规模开源数据集与 Benchmark

### 5.1 规模化数据采集与覆盖设计

项目通过采购商用真机云和移动测试资源，并结合自有真机采集大规模多端指纹数据。采集覆盖不以“总行数越多越好”为目标，而以独立 stable profile 和环境多样性为核心，形成品牌/机型家族、Android/API 版本、WebView/浏览器版本、设备来源、采集批次和时间范围等多维覆盖矩阵。

所有平台使用冻结的 FeatureApp、Browser Probe、字段目录和采集协议。每次采集同时记录原始载荷、逐字段状态、receipt、batch、pair provenance、采集器版本和载荷哈希；重复采集用于观察运行状态变化与时间稳定性，不重复计算为新的设备覆盖。云设备、自有真机、模拟器或调试环境只表示来源与环境事实，不自动等同于正常或攻击标签。

### 5.2 开源数据集的发布内容

数据集以版本化快照发布，并在供应商条款、数据许可和隐私要求允许的范围内开放以下内容：

- 脱敏和规范化后的 App177、Browser67、paired244 与 App-only 观测；
- 与特征值分离的逐字段可用状态、匿名 stable group、配对组和固定 split；
- 脱敏 SampleManifest、EvidenceBundle、事实标签、标签来源和质量状态；
- 字段注册表、Schema、特征目录、QC 报告、覆盖矩阵和数据构建 Manifest；
- 数据卡、用途说明、已知偏差、隐私处理、许可证、引用方式、版本记录和变更日志；
- 从获准公开的原始输入重建规范化视图、证据和评测结果的脚本。

数据采用“公开脱敏层 + 受控研究访问层”的两级发布方式。原始 session ID、IP、精确时间、设备槽位、供应商账号、长期设备标识和其他可关联信息不进入公开层；完整 build fingerprint、UA、Canvas/Audio/Font hash 等高可关联字段根据用途进行语义化、分桶、抑制或受控访问评估，不能假设普通哈希天然消除了反推风险。stable group 使用面向发布版本的伪标识，生成密钥不随数据公开。

采购设备服务不自动意味着取得数据公开再分发权。项目为每个来源维护 `source_rights_registry`，分别记录原始载荷、派生字段、日志和设备标识的发布权限、署名要求、研究用途限制和保留期限。只把许可明确的数据纳入开放数据集；受合同限制的记录仅用于内部质量评估、私有测试或发布聚合覆盖统计。数据集、评测代码和基线模型分别声明许可证，并提供更正、撤回、版本替换和安全联系机制。

### 5.3 Benchmark 任务轨道

开源数据集进一步配套形成统一 Benchmark，使不同方法在相同数据、相同划分和相同指标下比较。来源完备的 paired244 是完整性主任务单元；App-only 只进入采集覆盖、缺失鲁棒性或端侧部署补充轨道，不混入 paired244 主任务分母。

| 任务轨道 | 输入与目标 | 核心评价 |
|---|---|---|
| B1 跨层完整性识别 | paired244 或 EvidenceBundle；预测 `manipulation_present` 和 `violation_types` | AUPRC、Macro-F1、benign FPR、TPR@固定 FPR、多标签 F1 |
| B2 跨上下文配对推理 | 来源完备的 App 内 Web 与 Browser67 配对证据；识别可解释的一致、合法差异和经核验违规 | 各关系 Precision/Recall/F1、不可评估样本处理正确率 |
| B3 配对场景变化分析 | 同一 stable profile 的基线、操纵与恢复观测；定位变化字段并评价恢复一致性 | 配对分数差、字段定位 Precision/Recall、恢复一致率 |
| B4 OOD 泛化 | 未见 stable profile、品牌家族、平台来源、版本、外部场景家族或未来批次 | ID→OOD 相对下降、group bootstrap 置信区间 |
| B5 知识检索 | EvidenceBundle 查询与带标注的规则/知识卡相关性 | Recall@k、nDCG@k、MRR、容错与反例覆盖率 |
| B6 可验证推理 | EvidenceBundle + ContextPack；生成结构化判断、引用和理由 | Citation Precision/Recall、Unsupported Claim Rate、Invalid Trigger Rate |
| B7 系统与端侧效率 | 完整 Agent 与端侧轻量策略 | p50/p95 延迟、吞吐、token、成本、存储和失败率 |

B1–B3 中的操纵或违规真值只来自与载荷严格绑定、具有独立证据的事实 sidecar；Browser 字段不同、云设备来源或调试状态本身不能自动生成正例。外部受控场景在本项目中的职责是事实接入、质量校验、任务分流和评测，不包含攻击工具研发或攻击执行。

### 5.4 固定划分、基线与评测发布包

Benchmark 的官方划分按 stable profile、pair 和 scenario 整组完成，固定 train、development 和 test manifest；训练与开发数据用于建模和调参，测试组不参与规则发现、案例索引、Prompt、阈值或超参数选择。v1 发布前完成类别、来源和设备覆盖审计，冻结 split 生成器、种子和 Manifest；v1 发布后的新增数据只能形成兼容扩展版本或新的主要版本，不因新实验结果重新洗牌原测试组。公开发布提供可重复的参考测试集；在具备持续维护条件时，另保留标签不公开的挑战测试集，降低对公开测试标签的过拟合。

Benchmark 发布包至少包括：

- 固定任务定义、标签字典、纳入/排除规则和官方 split；
- 统一预测提交格式、自动评测器、group bootstrap 与统计检验脚本；
- 多数类、确定性规则、单端模型、传统模型、直接 LLM、Full-KB、Exact/BM25/Hybrid RAG、Verifier 和 Oracle retrieval 等参考基线；
- 每个基线的固定配置、版本、运行 Manifest、结果表和资源开销；
- Benchmark 数据卡、Leaderboard 规则、结果报告模板和版本兼容策略；
- `source_rights_registry` 与 artifact ownership/contributor matrix，区分本项目产生、外部提供、允许公开和仅用于私有评测的资产。

每个数据集与 Benchmark 版本同时公布 session、App177、Browser67、paired244、App-only、stable profile、可核验 physical device、scenario/pair group 以及品牌、系统版本、浏览器版本和来源覆盖，避免用单一 session 总量夸大数据规模。Benchmark 的价值不仅是给出一个排行榜，还在于形成可复现、可审计且能够长期扩展的公共研究协议。

## 六、知识增强 Agent 决策机制

### 6.1 跨层证据组织

系统不把 244 个字段直接作为一段无结构文本交给大模型，而是先由确定性程序完成版本、屏幕、GPU、UA、WebView 内核、JSBridge、传感器、自动化表面、运行时状态和 App/Browser 同名字段等关系比较。App177 的核心证据按 Native–Web、Native–WebView、WebView–Web、三层联合语义、物理运行环境和受控场景六组组织；Browser67 另外形成 App–Browser 跨上下文配对证据通道，只有比较语义和事实标签通过验证的证据才参与融合。

字段差异只表示两个采集面观察不同，必须结合字段稳定性、容器差异、权限域、时序、网络和合法反例进行解释，不能直接把不一致等同于攻击或欺诈。

### 6.2 分层知识库

知识库分为四层：

- **官方语义层**：解释 Android、Chrome、WebView 和 Web API 的字段含义、机制、限制及合法差异，不直接给出项目风险概率；
- **确定性关系层**：保存可转为程序谓词的跨层一致性规则和强制检查；
- **经验规则层**：保存仅由训练组归纳、经开发组和反例验证后发布的规律；
- **案例层**：保存训练组内经审核的证据案例，并排除相同稳定画像、相同配对、相同证据哈希和测试组派生样本。

### 6.3 字段感知检索与结构化推理

检索器以 EvidenceBundle 中的异常字段、证据组和场景为入口，先执行版本、状态和适用范围硬过滤，再组合字段精确映射、关键词检索与可选的稠密检索，对结果按字段重合度、证据强度、容错和反例覆盖进行重排。ContextPack 同时包含触发卡、容错卡和反例卡，避免只向模型提供单向的“高风险证据”。

大模型对各证据组输出结构化结果，包括组分数、引用字段、引用规则、支持证据、容错证据、理由和不确定性。最终总分权重不由 Prompt 隐式决定，而由可复现的外部融合模块学习或由确定性策略给出。

### 6.4 Verifier 与最终决策

Verifier 对模型输出执行以下检查：引用字段真实存在、引用规则或知识卡存在、规则触发条件满足、强制规则与 short-circuit 得到执行、未使用未来知识、解释没有引入载荷中不存在的事实、分组分数与风险方向一致、JSON 符合输出 Schema。

通过验证后，融合与校准模块生成精简的 `RiskDecision`，完整输出至少包括：

- 风险分、风险等级和不确定性；
- 命中的完整性违规类型与证据组分数；
- 被引用的字段、规则、知识卡和官方来源；
- 经过验证的风险原因与合法容错说明；
- 建议动作，如放行、增强认证、限权或人工复核；
- 完整 `DecisionTrace`，用于复现、错误分析和论文案例展示。

### 6.5 两级运行与端侧轻量输出

系统提供两种互补的运行形态。完整 Agent 在服务端或离线研究环境中完成知识检索、结构化推理、验证和审计；端侧运行形态把经过独立事实标签验证的低维证据、确定性策略或轻量评分组件冻结到 Android `riskapp`，在本地输出风险摘要，仅向后端上传分数、等级、模型版本和必要解释。端侧轻量组件属于延迟、隐私和离线可用性的工程部署基线，不作为新的分类算法贡献，并通过与独立事实、完整 Agent 决策、延迟、存储和资源开销的对照进行评价。

## 七、实验设计与预期形成的结果

### 7.1 事实标签与评价任务

实验事实分为环境事实、受控操纵事实、完整性违规类型和策略风险四层。主要评价对象是 `manipulation_present` 与 `violation_types`，风险总分属于策略输出。受控操纵样本必须通过标准化外部事实文件与 App 载荷哈希绑定，并包含成功证据、同设备配对关系和清理恢复证据；攻击构造和执行过程不属于本文工作范围。

### 7.2 防泄漏数据协议

数据首先建立 `stable_device_key`、配对关系和场景组，然后锁定 train、development、test；同一稳定画像、同一 clean/active/post 配对和同一场景不得跨组。经验规则、案例索引、数据增强、Prompt、检索参数、阈值和融合超参数只使用训练组或开发组，最终测试组在全部版本冻结后只运行一次。数据规模同时报告 session 数和独立 stable profile 数。分组切分用于阻止同一画像跨集合造成评价泄漏；`sample_weight = 1 / group_size`、证据哈希去重和训练组内受控扰动用于降低重复画像的训练偏置，二者不能相互替代。

### 7.3 基线、消融与泛化评价

正式比较包括多数类、确定性规则、单端特征、原始字段传统模型、显式跨层特征模型、无知识 LLM、整库 Prompt、Exact/BM25/Dense/Hybrid 检索、完整 RAG + Verifier + 外部融合以及 Oracle retrieval。消融实验分别移除特征层、跨层关系、官方知识、经验规则、容错与反例、字段过滤、重排器、Verifier、样本权重和外部融合，以识别每项收益来源。

泛化评价覆盖未见稳定设备、未见品牌或设备家族、未见工具或场景家族、未见 provider、未来采集批次以及只伪造部分字段的自适应场景。统计置信区间以稳定设备组或 pair 为重采样单位，而不是把重复 session 当作独立样本。

### 7.4 结果指标与论文证据

预期形成四类完整结果：

1. **检测与完整性结果**：AUPRC、Macro-F1、各场景 F1、benign FPR、固定 FPR 下的 TPR、Balanced Accuracy、配对分数差以及完整性违规 Micro/Macro-F1；
2. **检索结果**：Recall@k、Precision@k、nDCG@k、MRR、正确字段/规则覆盖率、容错与反例覆盖率、无关卡片率和上下文开销；
3. **解释与可信度结果**：Citation Precision/Recall、无依据结论率、虚构字段率、无效规则触发率、Verifier 拦截/修正率和人工盲评结果；
4. **系统与泛化结果**：ID 到 OOD 的性能变化、稳定设备组置信区间、校准误差、输出稳定性、p50/p95 延迟、token、单样本成本、吞吐和失败率。

实验不预设 RAG 或官方知识一定提高检测率。最终结果需要明确回答：跨层证据是否有效、知识的价值体现为检测增益还是语义约束、检索是否优于强基线、Verifier 是否降低无依据输出，以及系统能力能够泛化到哪些边界。即使某一模块只改善可审计性或只对特定场景有效，也形成有边界、可复核的研究结论。

## 八、最终预期成果

| 成果类别 | 最终交付内容 |
|---|---|
| 可运行系统 | 从 App177/Browser67 采集与来源配对，到 EvidenceBundle、知识检索、结构化推理、Verifier、融合校准和决策输出的端到端 HybridGuard 原型 |
| 大规模开源数据集 | 由多平台采购设备与自有真机形成的脱敏 App177、Browser67、paired244、App-only、字段状态、EvidenceBundle、固定分组和版本化数据卡 |
| 标准化 Benchmark | B1–B7 任务轨道、官方 split、统一指标、参考基线、自动评测器、统计脚本、提交格式、Leaderboard 规则和结果模板 |
| 数据与推理契约 | 版本化 Schema、字段注册表、SampleManifest、EvidenceBundle、KnowledgeCard、RiskDecision 和 DecisionTrace |
| 可治理知识库 | 具有来源、版本、适用范围、容错、反例和审核状态的官方知识卡、确定性规则、经验规则与训练内案例索引 |
| 可验证 Agent | 字段感知检索、结构化分组推理、引用验证、规则触发验证、外部融合和完整决策追踪 |
| 两级部署输出 | 服务端/离线完整 Agent、只读分析 API，以及在 Android 端运行且只上传风险摘要的轻量策略基线 |
| 实验结果包 | 强基线、检索对照、知识来源消融、模块消融、严格 OOD、校准、稳定性、效率与统计置信区间结果 |
| 复现与工程产物 | 冻结 run artifacts、构建 Manifest、配置与版本清单、自动化测试、离线命令、只读分析 API、典型案例和错误分析 |
| 论文材料 | 系统架构图、数据协议图、Agent 推理流程图、主要结果表、消融图、OOD 图、可靠性图、案例决策轨迹及论文正文 |

## 九、预期研究贡献

本项目面向整篇论文可归纳为三项主贡献：

1. **多端对齐与跨层证据表示**：提出面向 Hybrid App 与独立浏览器协同场景的来源完备配对方法，形成 App177、Browser67 与 paired244 的统一字段契约，并将原始指纹转化为保留字段状态、来源和版本边界的 EvidenceBundle；
2. **可追溯、可验证的知识增强 Agent**：构建由官方语义、确定性规则、经验证经验规则和训练内案例组成的分层知识体系，形成“检索—推理—验证—融合—追踪”闭环，使每个风险结论具有可程序校验的完整证据链；
3. **大规模开源数据集与标准化 Benchmark**：利用多平台采购设备和自有真机形成大规模、多来源观测，以 provenance、stable profile、配对关系、事实标签和 OOD split 为治理基础，发布固定任务、强基线、统一评测器和版本化数据卡。

三项贡献共同使用按 stable profile、pair 和 scenario 分组的防泄漏协议，使研究结论建立在独立设备画像、严格统计检验和 OOD 评价之上，而不是建立在重复 session 数量上。

## 十、建议的论文内容映射

| 论文章节 | 可整合的本项目内容 |
|---|---|
| 第1章 绪论 | Hybrid App 风险认证背景、单端指纹局限、研究问题与贡献 |
| 第2章 背景与问题定义 | 多端指纹、风险自适应认证、设备完整性任务、威胁模型与工作边界 |
| 第3章 多端采集与数据协议 | App177、Browser67、paired244、来源配对、字段状态、SampleManifest、QC 与稳定画像 |
| 第4章 开源数据集与 Benchmark | 规模化采集、覆盖矩阵、开放版本、固定任务、官方划分、基线、评测器和数据卡 |
| 第5章 跨层证据与知识构建 | EvidenceBundle、跨层比较、四层知识库、候选规则晋级与版本治理 |
| 第6章 字段感知检索与验证式 Agent | 检索、结构化推理、Verifier、融合校准、RiskDecision 与 DecisionTrace |
| 第7章 实验设计 | 事实标签、分组切分、防泄漏协议、基线、消融、指标与统计方法 |
| 第8章 实验结果与分析 | 主结果、Benchmark 结果、检索效果、知识与 Verifier 贡献、OOD、效率和失败分析 |
| 第9章 讨论与总结 | 数据偏差、能力边界、隐私与许可、系统部署价值、局限和扩展方向 |

## 十一、可供整合时直接引用的总结

HybridGuard 最终形成一套面向移动端风险自适应认证的多端设备完整性推理框架。系统通过商用真机云、移动测试平台和自有真机开展大规模、多来源采集，以 FeatureApp 获得 Android Native、WebView 宿主和 App 内 Web 共 177 项特征，并与独立系统浏览器的 67 项 Web 指纹进行来源完备配对，构成具有 244 项固定字段契约的正式观测单元。经过版本化 Schema、字段状态、SampleManifest、稳定画像分组、脱敏和 QC 后，项目发布带固定任务、官方 split、强基线、统一指标、自动评测器和数据卡的开源数据集与 Benchmark。在推理侧，系统将原始指纹转换为可复现的跨层 EvidenceBundle，利用官方语义、确定性规则和经审核经验知识进行字段感知检索，由大模型完成结构化分组推理，再通过 Verifier、外部融合与校准输出可审计的 RiskDecision 和 DecisionTrace。项目通过防泄漏切分、模块消融、OOD 和系统效率测试，评价跨层证据、知识检索和结果验证对检测能力、解释可信度与泛化边界的影响，最终交付可运行原型、大规模开源数据集、标准化 Benchmark、可治理知识资产、完整实验结果和可复现论文材料。

# P6 冻结评价协议

协议：`mtc-p6-fixed-comparison-v1`；声明时间：`2026-09-22T16:24:47.323112+00:00`。正式运行会先保存代码、规则、协议和访问记录，再读取保留集字段。

本轮只比较现有 MTC 数据上的可评估关系、偏差、上下文、未知与运行成本。不训练模型、不改阈值、不补采，不报告准确率、误报率、召回率、F1 或真实攻击检测收益。

## 同一数据与分组口径

固定 P2 的 891 条厂商／型号／Android release 主代表：发现 630 条／610 组，开发 144 条／142 组，保留验证 117 条／116 组。主代表在 P2 已按最早合格采集选定，不能按本次输出换样本。

主要均值先在每个防泄漏组内平均，再对组平均。逐记录计数同时保存。失败保留原分母，并使对应整体均值／区间保持不可计算，不将失败改成无偏差。未知、不适用与未评估分别统计。

保留集在 P2 前已暴露全量 QC 信息，不是从未查看的外部盲测集。P2 原始 LOCKED 文件不改写；P6 通过单独访问记录执行固定评价，P3 入口仍拒绝读取保留集。一次性访问记录存在时，正式入口拒绝静默另开同协议运行。

137 条 paired_244 重复观测仅运行 Full244，与既定同型号／系统主代表比较；同安装标识另列，不宣称物理设备时间稳定性。654 条 App-only、11 条 partial 和 6 条同 session 额外记录保留在数据/QC 分母，未加入完整配对主矩阵。

## 15 个固定比较条件

| 条件 | 内容 | 目录中活跃检查数 |
|---|---|---:|
| `Native84` | Native 单层 | 57 |
| `Host26` | WebView 宿主单层 | 57 |
| `AppWeb67` | App Web 单层 | 57 |
| `Browser67` | 独立 Browser 单层 | 57 |
| `NativeAppWeb151` | Native＋App Web | 57 |
| `AppWebBrowser134` | App Web＋Browser | 57 |
| `App177` | App 三层 | 57 |
| `Full244` | 全部四层 | 57 |
| `Full244_no_cross` | 全部输入，去掉所有 App—Browser 关系 | 49 |
| `App177_v1` | P4 首轮修订目录 v1 | 48 |
| `App177_v2` | 首批旧条目处置目录 v2 | 54 |
| `Full244_empirical` | 经验来源 | 23 |
| `Full244_official` | 官方派生来源（含经验筛选） | 23 |
| `Full244_empirical_official` | 经验＋官方派生 | 46 |
| `Full244_support` | 部署策略、采集上下文与自检 | 11 |

每个输入视图在构建证据前移除其他层的字段、状态和质量信息；规则、容差和解析器不变。子目录只删除不属于预先声明范围的活跃检查，不改剩余谓词。所有源目录、选择后的准确 ID 与代码副本写入运行目录。

跨端消融删除所有同时依赖 app.* 和 browser.* 的 8 项关系，包括 Native—Browser；不是仅按规则名称删除。其余 Browser 单端检查仍保留。

P4 v1 的 48 项已经包含 P3 的修订和新增，不能将它冒称原始旧引擎。E1 另外重放原始 10 项已编译经验谓词和 9 项官方派生关系，保留顺序短路、独立诊断和可用性门控结果。

当前 23 项官方派生检查中，14 项经过 P3 的数据筛选；不能把这一组描述为完全不依赖数据的官方规律。经验和官方可能共享同一证据家族，分别报告重合，不能将联合数量当独立投票数。

## 固定比较和不确定性

主对照包括 App177→Full244、Browser67→Full244、去跨端→Full244、Native＋App Web→App177、App Web＋Browser→Full244、v1/v2→当前 App177，以及来源单组→联合。完整清单见机器协议。

保留集对照在同一分组上成对计算差值；固定随机种子 620260923、2,000 次组级自助重采样，报告可评估检查数、存在关系偏差比例、未评估检查数的条件 95% 区间。不使用这些区间选规则、阈值或最佳版本，不作总体泛化或显著性胜负结论。发现／开发仅报告描述统计。

所有厂商原字符串、API、APK 版本和浏览器包名分层均保存；不足 10 个组仅作稀疏描述。没有依结果新建机型族、白名单或留出分组。

每个单元执行一次并经过内置 Verifier。每条样本循环移位比较条件的运行顺序，记录本机离线全链路耗时中位数／p95；这是包含 Verifier 的一次运行，不是手机端延迟或最优性能基准。

## 历史材料的有限诊断

仅尝试既有固定 input-set 指定的清单与 sibling raw；不得下载、补采或换入新样本。除了原有两态准入，还要求明确完整的 177 项字段状态，才能映射到当前执行器；不从旧数值推造 observed 状态。

可兼容时只比较同一旧输入在新旧实现上的输出，全部另列，不并入 MTC，不继承旧标签或效果目标，不拼接 Browser。缺文件、证据或格式不兼容时写明原因并关闭，本轮不等待补齐。

## 产物与复核

正式目录为 `hybridguard_agent/artifacts/mtc_p6_fixed_20260923/`。保存完整逐单元结果、全部规则反例、原始旧引擎输出、分组汇总、分层、重复观测、历史诊断、冻结代码／目录和保留集访问记录。

运行失败也保留日志；禁止删除访问记录后重跑并选择结果。保留集结果不会回写本轮规则目录。后续若做技术复现，应明确登记为复现，不能冒称新的独立验证。

对应入口：`hybridguard_agent/scripts/run_mtc_p6.py`；机器协议：`hybridguard_agent/config/mtc_p6_protocol.v1.json`。

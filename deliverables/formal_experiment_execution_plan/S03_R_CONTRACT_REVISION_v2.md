# 执行计划的 S03-R 合同修订 v2

修订版本：`formal-experiment-plan-amendment-s03r-v2-20260923`。适用原计划：`formal-experiment-execution-plan-v1-20260923`。审查基线：`66f9cdc2d40c41ccec0999c7a6c8d68c0a1b415f`。本修订完成工程验证后等待审查；不授权或启动 S04。

## 1. 原记录与修订边界

S01/S02 不重跑，不重新裁决标签或准入。S03 v1 来源、家族、八组合、文档审查、测试和零候选验收保持历史事实。S03-R 单独登记到 `EXECUTION_STATUS.json.step_revisions`；不把 S03 v1 的验收结果改成 7 条候选，也不覆盖冻结 v3/P0–P6。

新配置：`hybridguard_agent/config/formal_manipulation_role_gate_v2/`。

新产物：`hybridguard_agent/artifacts/formal_manipulation_v1_20260923/03r_role_gate_v2/`。

原 S03 生成器只修正写入入口：output 与 config_dir 必须同时显式指定，拒绝旧目录、非空目录、重叠路径及解析到保护范围的符号链接。v1 角色和门控行为及原测试保留。

## 2. 对原计划 5.2 的版本化修订

后续经单独授权的 S04 应显式绑定 `formal-manipulation-relation-risk-attribution-v2` 与 `featureapp-reported-identity-coherence-research-v2`，并使用新版 decision_roles/applicability/source_conditions/family_bindings。旧 source_registry 仍是来源依据，旧嵌入角色或旧归因前提不得覆盖 v2 的审定资格。没有自动回退或按来源混用版本。

三个轴分开：A 判断关系是否可比较；B 判断是否具备限定研究用途的风险候选资格；C 判断攻击、授权或意图能否归因。C 在当前允许字段下保持 UNKNOWN，不自动把 A/B 压成未知，也不能因关系冲突而晋级。

原条款“合法替代解释完全排除后才可作为候选”改为：保留真实影响测量含义的必要门控；独立记录用于强归因的条件；具备既有语义/研究依据的关系可在统一声明范围内成为待评价风险线索。缺字段、类型无效、非有限值、零哨兵、缩减/掩蔽身份、错误产品语义、软件与硬件族混比等仍不能放宽。缺风险参照不等于缺关系操作数，两者分别报告。

审定风险候选为 NW-001、NW-002、NW-005、NVW-001、NVW-002、OFFDER-OS-001、OFFDER-OS-002，共 7 条、5 个家族。OFFDER-GPU-001 仍观察，其后端词不足以支持独立身份不一致判断。其他 49 条未建议规则的既有角色不变；不把 ADB、模拟器、debug、部署不符等变成攻击票。

default/settings 一致、default 与 Native 型号/版本相符是保守的当前风险参照条件，不证明连续来源。合法覆盖、OEM 差异和渲染路径变化仍可造成误报；必须保留它们的独立合法标签，不能为了提高检出率或减少误报改标签。研究范围事前统一，不能从工具、配置、阶段、日志或事实侧按样本填入。

## 3. S04 的指定接口与尚未实现部分

只有 A=SUPPORTED、B=ELIGIBLE 且 verifier/字段要求通过的候选关系，才可进入后续家族评估。关系一致与冲突均保留独立 outcome。观察、上下文、采集器和部署角色不能进入风险家族票数。

原计划的同家族归并、固定阈值 1、NO_ALERT/INSUFFICIENT_EVIDENCE/FAILED 边界仍是 S04 的待实现设计；本步没有实现聚合器、计分、风险报警或更改阈值。将来报警只表示“声明研究范围内疑似操纵／风险提示”，不是攻击真值、未经授权或恶意意图的证明，概率保持 null。没有候选可评估时不能用 NO_ALERT 代替弃判。

S03-R 仅提供单关系的 A/B/C 与有限合成 probe。32 项聚焦测试（原 v1 15、新 v2 17）及 35 个保存合成夹具证明代码路径可达；不证明 S04 已实现、真实样本中必有可评估候选或已有检测能力。

## 4. 对原计划 5.3 的限制补充

57 个 ID 的来源归属、27 个家族、公共 C 和八个来源 ID 集不变；全部条件统一使用 v2 门控与同一研究范围。E 有 5 条候选/5 个家族，O_u 有 2 条/2 个家族，H/C 没有候选。

O_u 的 app_os/host_os 与 E 同族表达在共同有效域及相同 v2 条件下等价。保留 E 时，O_u 不增加候选家族；在原计划的固定家族 OR 逻辑下，零二值增量存在事前结构原因。H/C 也不能用无报警角色下的零增量证明来源无价值。规则数、文档引用数及来源数不增加独立支持票。

删除 O_u/H 谓词但保留公共语义门控，仍是来源分组消融，不能称完全移除官方知识。八组合的观察覆盖可以不同，实际风险覆盖或效果尚未评价。

## 5. 仍不可评估的检测主张

- 真实 TPR/FPR、检测准确度、校准概率和任何来源的真实收益，本步均 NOT_EVALUATED。
- S01 时间对照 no_intervention 仍 UNKNOWN，不能进入对应 FPR 分母；S01 其他阶段既有资格原样保留。
- 未知执行归因、缺原始日志、L1 证据上限与环境关联组不是独立物理设备的限制未消除。
- 当前 App177 没有独立 Browser 攻击输入，不能拼接或补造 Full244 检测结果。
- 不能推断恶意意图、未经授权来源、可信物理身份、不可观测干预或跨独立设备泛化。

S03-R 完成后停止，等待用户审查；S04 仍为 PENDING_AUTHORIZATION。本次不提交、不推送。

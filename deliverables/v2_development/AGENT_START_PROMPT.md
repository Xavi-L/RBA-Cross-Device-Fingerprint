# 发给 agent 的 V2-A 启动 prompt

在 RBA-Cross-Device-Fingerprint 工作区开始 V2-A：
“漏检诊断、字段参照核实与首轮平铺/联合表示开发试验”。

我暂缓 R10。V1保留为基线，现在的主任务是开发和改善检出—clean报警—覆盖权衡。
本轮允许完成一个连贯的开发批次，不需要每写一个脚本就停下来请求确认。
只执行V2-A；完成后停止，不自动进入V2-B/C或R10。

一、读取和安装

先读取适用的AGENTS.md，然后完整阅读：
- V2_DEVELOPMENT_ROADMAP.md
- deliverables/v2_development/EXECUTION_PLAN.md
- deliverables/v2_development/DEFAULT_DEV_CONTRACT.json
- deliverables/v2_development/EXECUTION_STATUS.json
- deliverables/v2_development/SOURCE_REGISTER.json

若这些文件来自我提供的ZIP，按包内相对路径放入仓库；只新增文件。
遇到同名文件先比较，不直接覆盖已有V2工作。
事实基线是5aabe2d609ab6ab737a7a057d4b9260479385fad，允许其后继提交。
检查工作区，保留用户修改，不reset/clean/stash，不回退到历史HEAD。

按V2执行计划仅做一次轻量路由：旧主线/计划顶部追加V2指针和R10暂停；
原可变状态先按原字节归档，再更新当前调度，保留历史步骤。
不修改冻结快照，不重新做R01–R09，不为旧PARTIALLY_COMPLETED补支线。

二、本轮授权

允许只读分析现有数据，编写V2最小适配/诊断代码，运行合成测试，
以及以下两个规格各3折LOEO、总计最多6次真实fit：
1. S_FLAT：三个表面的既有单表面原子/控制条件平铺，无新增显式跨层关系。
2. J0：S_FLAT加V1的C0规范跨层关系，统一训练规则集合。

真实计算采用GREEDY_OR/OP05，主要目标、阈值编码、支持度、复杂度、
seed和并列规则按DEFAULT_DEV_CONTRACT绑定的V1设置，不临时调参。
失败后重试也计入6次，总执行计费上限600秒且不超过实际剩余额度。
先核对原研究账本及R09关联扣账，V2另记关联账本，不归零历史消耗。
文档里的初始pending状态由本次明确指令解除，仅限V2-A。

不授权新采集、攻击工具、付费服务、任意字符串/ID特征、新谓词、
IP/DNF2/列生成实验、全开发集最终拟合或独立确认集访问。

三、先定位问题，但不要再次只交付计划

复用R02矩阵、R05–R08已保存预测/训练日志和S01/S02元数据。
覆盖全部14个配置，重点检查跨层漏检30、单层补检12、共同漏检18，
同时保留已检出记录和相应pre/post，不只对漏检样本造规则。

按路线图分类：未观测到变化、无跨层参照、表示缺口、测量不可用、
支持度排除、clean/复杂度约束、搜索限制、分布变化或工程问题。
每项提供证据路径与后续动作；不把所有问题一律归为缺跨层rule。

核实最值得研究的最多3个关系家族是否有真实Native/Host/Web参照。
字段不存在、单位不同或合法变化未厘清时明确记录；不凭名称补造字段。
检查名义跨层规则的Native锚点是否实际恒定或只是有效性门控。
可以分析固定表示下的同输入异标签组，但只作为开发可分性诊断。

四、执行首轮试验

从C0/W0的已关闭同折预测计算事后OR诊断，检查全部162阶段，
不只合并攻击ID；标POST_HOC_ENSEMBLE_DIAGNOSTIC，不叫最终模型成绩。

V1已保存C0/W0不重训。实现S_FLAT/J0所需的最小V2适配，复用原内核；
不能用synthetic=True、伪造V1授权或修改旧模型JSON绕过权限。
新入口明确使用V2身份和本次批次授权，不重建整套训练平台。

先做合成测试：精确候选集合、别名/极性、T/F/U、train-only数值fit、
模型保存加载、预测不读标签及失败保留；随后运行获准的6次以内拟合。
只向fit传训练成员；编码器/模型冻结后再变换并预测本折评价成员。

每次试验前保存batch_spec，所有模型、阈值、规则、预测和失败留痕。
开发期确定工程错误可以修复后留痕重跑，但不删除原尝试、不择优覆盖。
有效的低检出/空模型是结果，不触发更换参数自动重试。

五、交付和停止

在hybridguard_agent/artifacts/discriminative_rule_learning_v2_20260925/A_diagnosis_pilot/
保存诊断、字段参照表、batch_spec、trials、必要模型/预测/解释及统一指标。
报告两种新表示相对C0/W0的：宏/微检出、pre/post报警、覆盖、弃判/失败、
复杂度、逐配置结果、新增与丢失检出；保留所有不利结果和未知状态。

用一份REPORT.md回答：瓶颈在哪里，S_FLAT/J0有没有增益，
哪些原始信号没有被跨层表达，下一批应优先研究什么及其合法反例。
最多推荐3个关系家族；若无改进，解释原因，不强行提交更高TPR。
这些都是已暴露材料上的开发结果，不是新盲测。

不要增加一套十阶段验收链，不重做历史全库审计，不要求先补齐R10。
在本轮可完成范围内直接作出合理实现选择，确实阻塞的分支明确报告。
完成后更新V2-A状态并停止，等待我审查；不自动commit、push或创建PR。
